# 모델 구성

i-Route AI 서버가 서빙하는 모델과 학습 내역을 정리한 문서입니다.

> 어댑터 실물(약 667MB)과 학습 데이터(200MB+)는 용량 때문에 `.gitignore`
> 대상입니다(`train/*`, `train/*.py`만 예외). 저장소에는 학습·전처리·평가
> 스크립트만 있고, 이 문서가 그 결과를 기록하는 역할을 합니다.

---

## 서빙 구조

베이스 모델 **하나**에 LoRA 어댑터를 올려두고, 요청마다 `set_adapter()`로
갈아끼웁니다. 어댑터는 6개를 학습했지만 **지금 로드하는 것은 math 하나**이고,
그것도 `PeftModel`을 만들기 위한 것일 뿐 요청 경로에서는 쓰지 않습니다. 실제
생성은 전부 베이스(`disable_adapter()`)나 Ollama가 합니다.

```
unsloth/Qwen3-8B-unsloth-bnb-4bit  (4bit NF4, bf16 compute, double quant)
├── math     train/math_adapter_qwen              로드함 (PeftModel 생성용, 요청 경로에서는 안 씀)
├── writing  train/writing_adapter_qwen_weighted  로드 안 함 (LOAD_WRITING_ADAPTER=False)
├── korean   train/korean_adapter_qwen            로드 안 함 (LOAD_SUBJECT_ADAPTERS=False)
├── english  train/english_adapter_qwen           〃
├── science  train/science_adapter_qwen           〃
└── social   train/social_adapter_qwen            〃
```

### 실제로 호출되는 경로 (2026-09-23, Backend·Front 코드 기준)

Backend(`I-route1/Backend`)가 AI 서버에 보내는 요청과 각 요청이 쓰는 모델입니다.
Front(`I-route1/Front`)는 AI 서버를 직접 호출하지 않습니다.

| 경로 | 쓰는 모델 |
|---|---|
| `/api/ai/report/subject-recommend` | 베이스 Qwen (`_concept_explain`), 한국사만 Ollama |
| `/api/ai/report/{영어·과학·사회·한국사}` | 베이스 Qwen (`_concept_explain`), 한국사만 Ollama |
| `/api/ai/report/math`, `/report/writing`, `/report/premium` | 규칙 기반 + **Ollama** `llama3.1` |
| `/api/ai/search`, `/api/rag/search` | RAG만 (생성 없음) |

**`/api/writing/*`(evaluate·irt·curriculum·compare·weakness-report)는 어느
클라이언트도 호출하지 않습니다.** Backend의 "writing"은 국어 리포트
(`/api/ai/report/writing`)를 가리키는 이름이고, 서술형 답안을 제출받는 화면이나
API는 Backend·Front 어디에도 없습니다. Backend의 `MathAiService`는 Ollama를 직접
부릅니다.

그래서 글쓰기 어댑터를 로드하지 않습니다(`LOAD_WRITING_ADAPTER=False`, 약 167MB).
라우터는 그대로 살아 있어, `/api/writing/evaluate`는 규칙 기반 점수·피드백을 내고
`llm_score`/`llm_score_raw`는 `None`입니다. 서술형 제출 기능이 생기면 `True`로
바꾸면 됩니다.

설정은 `src/api/adapters.py`에 모여 있고 `src/api/main.py`가 기동 시 로드합니다.

**왜 베이스를 하나로 합쳤나** — 예전에는 수학이 Gemma4, 글쓰기가 원격 Llama3.1
어댑터라 베이스가 서로 달랐습니다. 8B 모델 두 개를 동시에 띄워야 해서 VRAM이
부족했고 OOM이 났습니다. 두 어댑터를 같은 Qwen3-8B 베이스로 재학습해 합쳤습니다.

**리소스** (RTX 5070 Ti, 16GB 기준 실측)

| 항목 | 값 |
|---|---|
| 4bit 베이스 | 약 6 GB |
| 어댑터 6개 | 약 667 MB (개당 166.6 MB) |
| 합계 | 약 7 GB |
| 어댑터 6개 로드 시간 | 4.7초 |
| 개념 설명 1회 생성 | 5~15초 (200 토큰 기준) |

GPU 생성은 블로킹이라 FastAPI에서 `run_in_threadpool`로 분리해 호출합니다.

---

## 어댑터별 학습 내역

공통 LoRA 설정: **r=16, alpha=32, dropout=0.05**,
target_modules = `q_proj, k_proj, v_proj, o_proj, gate_proj, up_proj, down_proj`
(7개). 옵티마이저 AdamW, LR 2e-4, warmup 5%, batch 1 × grad_accum 16.
학습 루프는 HF Trainer가 아니라 순수 PyTorch 커스텀 루프이며,
eval_loss가 갱신될 때만 체크포인트를 저장합니다.

| 어댑터 | 학습 데이터 | 학습/평가 건수 | epochs | max_len | best eval_loss |
|---|---|---|---|---|---|
| math | AI-Hub 수학교과문제풀이 | 12,640 / 1,755 | 2 | 640 | **0.4381** |
| writing | 글쓰기 평가·피드백 | 28,500 / 1,500 | 3 | 896 | **0.3991** |
| korean | AI-Hub 교육과정(국어) | 15,200 / 800 | 2 | 768 | **0.3215** |
| english | AI-Hub 교육과정(영어) | 15,200 / 800 | 2 | 768 | **0.3247** |
| science | AI-Hub 교육과정(과학) | 15,200 / 800 | 2 | 768 | **0.3529** |
| social | AI-Hub 교육과정(사회) | 15,200 / 800 | 2 | 768 | **0.3066** |

### 프롬프트 형식

전부 Qwen3 채팅 템플릿(system / user / assistant)을 씁니다. 추론 시
`apply_chat_template(..., enable_thinking=False)`를 **반드시** 넘겨야 합니다.
학습 데이터에 `<think>` 블록이 없어서, 켜두면 모델이 빈 사고 블록을 먼저
내보내며 파인튜닝된 동작이 어긋납니다.

system 프롬프트는 학습 때 쓴 문구와 **글자 단위로 같아야** 합니다.
`src/api/adapters.py`의 값과 `train/preprocess_curriculum.py`의 `SUBJECTS`가
일치해야 하며, 한쪽만 고치면 성능이 떨어집니다.

교과 어댑터 입력 형식:

```
[학습 지문]
{지문}
[교육과정 성취기준]
{[9국03-01] 형태의 성취기준}
질문: {질문}
```

---

## 평가

### 교과 어댑터 (국어·영어·과학·사회)

`test_subject_adapters_eval.py`로 측정합니다. 같은 샘플을 **어댑터 적용**과
**어댑터 비활성(=베이스 모델)** 두 조건으로 생성해 비교합니다. 그 차이가
파인튜닝의 순수 기여분입니다.

한국어는 교착어라 어절 단위 토큰화가 불안정해 **문자 단위**로 잽니다.
평가는 greedy 디코딩(`do_sample=False`)이라 재현 가능합니다.

- `char-F1` — 정답/생성의 문자 다중집합 겹침 F1 (SQuAD F1의 문자판)
- `ROUGE-L` — 최장공통부분수열 기반 F1
- `EM` — 공백 제거 후 완전일치

```
python test_subject_adapters_eval.py --n 40
```

**결과** (과목당 40건, `--max-new-tokens 128`)

| 과목 | char-F1 (어댑터 / 베이스 / Δ) | ROUGE-L (어댑터 / 베이스 / Δ) | EM (어댑터 / 베이스) |
|---|---|---|---|
| 국어 | 0.836 / 0.283 / **+0.553** | 0.816 / 0.257 / **+0.559** | 0.375 / 0.000 |
| 영어 | 0.737 / 0.305 / **+0.432** | 0.724 / 0.283 / **+0.441** | 0.225 / 0.025 |
| 과학 | 0.692 / 0.357 / **+0.335** | 0.633 / 0.277 / **+0.356** | 0.125 / 0.000 |
| 사회 | 0.758 / 0.306 / **+0.453** | 0.714 / 0.244 / **+0.471** | 0.200 / 0.000 |

4과목 모두 어댑터가 베이스를 앞섭니다 (평균 char-F1 Δ **+0.443**).
베이스 모델의 EM이 사실상 0인 것이 특징적입니다. Qwen3-8B는 일반 지식으로
그럴듯한 문장을 만들지만, 교과서 답안의 표현 방식과 길이를 맞추지는
못합니다. 파인튜닝이 지식보다 **출력 형식과 답변 톤**을 학습시킨 셈입니다.

과학의 향상 폭이 가장 작습니다(+0.335). 베이스 char-F1도 과학만 0.357로
높은데, 과학 답안이 일반 상식과 겹치는 부분이 많아 베이스가 원래 어느 정도
맞히기 때문으로 보입니다.

### 글쓰기 어댑터

서술형 답안에 1~5점을 매기는 채점 정확도로 평가했습니다.

| 버전 | 정확도(±0) | ±1 이내 | 평균 오차 | best eval_loss |
|---|---|---|---|---|
| **weighted (서빙 중)** | **51%** | 99% | 0.50 | 0.3991 |
| weighted_v2 | 44% | 96% | 0.60 | 0.4012 |

`weighted`는 채점 클래스 불균형을 손실 가중치로 교정한 버전입니다
(sklearn `balanced` 방식, 상한 5.0). 채점 샘플에만 가중치가 걸리고 서술형
피드백 샘플은 1.0이며, eval 손실은 가중치 없이 계산해 다른 실행과 비교
가능하게 했습니다.

`v2`는 여기에 1점 샘플 오버샘플링을 더한 버전입니다. 1점이 전체의 2.7%(386개)
뿐이라 가중치만으로는 한 번도 못 맞히는 문제를 풀려는 시도였는데, 정확도가
51%에서 44%로 떨어졌습니다. 서빙에는 `weighted`를 씁니다.

#### 위 평가의 문제 (재설계함)

이 수치들은 아래 세 가지 이유로 신뢰하기 어렵습니다.

1. **표본이 100건뿐**입니다. 평가셋에 채점 샘플이 777건 있는데 100건만 썼고,
   그중 1점은 **4건**입니다. "가중치도 오버샘플링도 1점을 못 맞힌다"는 결론이
   4건 위에 서 있었습니다. 1점 클래스는 사실상 측정된 적이 없습니다.
2. **채점을 샘플링으로 생성**했습니다(`temperature=0.4, do_sample=True`).
   1~4 중 하나를 고르는 결정적 분류에 무작위성을 넣은 것입니다.
3. **정확도를 주지표로 썼습니다.** 점수는 순서형이라 3점을 2점으로 틀린 것과
   1점으로 틀린 것의 무게가 다른데 정확도는 구분하지 못합니다.

3번이 특히 중요합니다. 기존 혼동 행렬로 **QWK(quadratic weighted kappa)** 를
다시 계산하면 **0.530**이 나옵니다. QWK는 자동 채점(AES)의 표준 지표로,
예측이 정답에서 멀수록 크게 벌점을 주고 우연 일치를 보정하며,
**항상 같은 점수만 찍는 모델은 정의상 0**입니다.

| 조건 | QWK | 정확도 | 평균오차 |
|---|---|---|---|
| weighted (서빙 중) | **0.530** | 51.0% | 0.500 |
| 항상 3점 (상수 예측기) | 0.000 | 49.0% | 0.550 |

즉 정확도 51%는 상수 예측기(49%)보다 겨우 2%p 높아 보였지만, 실제로는
어댑터가 순서형 구조를 제대로 학습한 상태입니다. **지표가 성능을 가리고
있었습니다.** `test_writing_grading_eval.py`가 이를 바로잡습니다 —
전체 777건, greedy 디코딩, QWK 주지표, 클래스별 정밀도/재현율,
상수·사전분포 기준선 비교.

#### 재측정 결과 — 위 두 결론 모두 사실이 아니었습니다

```
python test_writing_grading_eval.py --logit-sweep      # 전체 777건
```

**(1) "1점을 한 번도 못 맞힌다"는 틀렸습니다. 1점 재현율은 0.733입니다.**
평가 표본의 1점 15건 중 11건을 맞힙니다. 기존 결론은 1점 4건을 모두
틀린 것을 보고 내린 것이었습니다. 가중 손실은 이미 제 역할을 하고 있었습니다.

**(2) 정확도 51%도 틀렸습니다. greedy로 재면 56.9%입니다.**
차이는 전부 디코딩입니다. 기존 평가가 채점에 `temperature=0.4` 샘플링을
쓰고 있었습니다.

| 어댑터 / 디코딩 | QWK | 정확도 | 평균오차 | 1점 재현 | 2점 재현 |
|---|---|---|---|---|---|
| weighted + argmax **(현재 서빙)** | 0.543 | 56.9% | 0.445 | 0.733 | 0.574 |
| weighted + 기댓값 | 0.553 | 58.7% | 0.423 | 0.733 | 0.557 |
| v2 + argmax | 0.534 | 52.5% | 0.501 | 0.667 | 0.721 |
| **v2 + 기댓값** | **0.569** | **61.3%** | **0.393** | 0.600 | 0.612 |
| 항상 3점 (상수 예측기) | −0.000 | 56.2% | 0.457 | 0.000 | 0.000 |
| 사전분포 무작위 | 0.040 | 38.6% | 0.734 | 0.000 | 0.251 |

**(3) v2가 "실패"했다는 판정도 과했습니다.** 정확도만 보면 51% vs 44%로
크게 뒤졌지만 QWK는 0.543 vs 0.534로 근소하고, 2점 재현율은 v2가 더 낫습니다.

**(4) 재학습 없는 개선이 있습니다 — 확률 기댓값 디코딩.**
argmax 대신 점수 분포의 기댓값을 반올림하면 **두 어댑터 모두에서** QWK가
오릅니다(0.543→0.553, 0.534→0.569). 순서형 과제에 argmax가 맞지 않았던
것입니다. `v2 + 기댓값`이 QWK 0.569, 정확도 61.3%, 평균오차 0.393으로
가장 좋습니다.

> 단서: `writing_eval_qwen.jsonl`은 학습 중 체크포인트 선택에도 쓰인
> 셋이라 완전한 홀드아웃이 아닙니다. 절대 수치는 다소 낙관적일 수
> 있습니다. 다만 argmax→기댓값 개선이 서로 다른 두 어댑터에서 같은
> 방향으로 나온 점은 디코딩 선택이 표본에 우연히 맞은 것이 아님을 시사합니다.

#### 그런데 이 채점 능력은 서빙에서 쓰이지 않습니다

위 수치를 개선해도 제품은 바뀌지 않습니다. `/api/writing/evaluate`의
`final_score`는 어댑터가 아니라 **규칙 기반**으로 나옵니다.

```python
raw_score = int(kw_score * 0.7 + length_score * 0.3) if req.keywords else length_score
```

`_keyword_score`(키워드 가중 일치율)와 `_length_score`(모범답안 대비 길이
비율)의 가중합인 0~100점입니다. 글쓰기 어댑터는 `_llm_feedback()`에서
**피드백 문장 생성에만** 쓰이고, 1~5점 채점 경로는 호출되지 않습니다.

그래서 기댓값 디코딩도 서빙에 반영하지 않았습니다. 바꿀 호출부가 없습니다.

**진짜 격차는 여기입니다.** QWK 0.54로 사람 채점과 중간 수준으로 일치하는
파인튜닝된 채점기를 두고, 서빙은 키워드 개수와 글자 수로 점수를 매기고
있습니다.

##### 규칙 기반 점수는 얼마나 신호를 담고 있나

```
python test_writing_grading_eval.py --baseline-analysis     # GPU 불필요
```

평가셋에 키워드·모범답안이 없어 키워드 항목(가중치 70%)은 재지 못했지만,
**길이 항목(30%)은 잴 수 있습니다.**

| 실제 점수 | 답안 길이 중앙값 |
|---|---|
| 1점 (n=15) | 913자 |
| 2점 (n=183) | 854자 |
| 3점 (n=437) | 883자 |
| 4점 (n=142) | 1191자 |

**1점 답안이 2·3점보다 깁니다.** 길이와 점수의 피어슨 상관은 **0.104**로
사실상 신호가 없습니다.

| 예측 방법 | QWK | 정확도 | 평균오차 |
|---|---|---|---|
| 길이 4분위로만 예측 | 0.098 | 29.3% | 1.017 |
| 항상 3점 (상수) | −0.000 | 56.2% | 0.457 |
| **파인튜닝 채점기** | **0.553** | **58.7%** | **0.423** |

길이 항목은 거의 잡음입니다. 키워드 항목이 얼마나 받쳐주는지는 이 데이터로
알 수 없지만, 서술형 답안의 질을 키워드 포함 여부로 가르는 데에는 한계가
분명합니다.

##### 조치

연결하려면 1~4점을 0~100점으로 어떻게 매핑할지, 기존 점수 분포와의 호환을
어떻게 할지 정해야 하고, 그건 **실제 학생 점수가 바뀌는 일**입니다.
그래서 `final_score`는 그대로 두고 채점기 의견을 **선택 필드로 함께**
보냅니다.

```python
llm_score: Optional[int] = None        # 1~4 반올림값
llm_score_raw: Optional[float] = None  # 반올림 전 기댓값 (상관분석용)
```

기존 필드가 그대로라 하위 호환이 깨지지 않습니다. 실제 트래픽에서 두 점수를
비교해본 뒤 교체 여부를 정하면 됩니다.

> **2026-09-23 확인: 그 "실제 트래픽"이 없습니다.** Backend와 Front 모두
> `/api/writing/*`를 호출하지 않아, `final_score`든 `llm_score`든 학생에게
> 닿지 않습니다. 맨 위 "실제로 호출되는 경로" 참고. 디코딩은 측정에서 더 나았던
기댓값 방식을 씁니다(순전파 1회라 생성보다 쌉니다).

실제 호출 확인: 비속어 섞인 부실 답안 **1점**(기댓값 1.039), 논리적인
답안 3점(2.669).

#### 1점 클래스: logit adjustment은 불필요합니다

가중치와 오버샘플링이 모두 학습 단계 개입이었던 것과 달리,
`--logit-sweep`은 **추론 단계**에서 해결을 시도합니다. 점수 토큰(1~4)의
로짓을 순전파 한 번으로 직접 읽은 뒤, 오프라인에서 디코딩 전략을 비교합니다.

- **logit adjustment**: `logit_c - tau · log(prior_c)` — 롱테일 분류의 표준
  보정으로, 다수 클래스 편향을 사전분포만큼 걷어냅니다. 재학습이 필요 없습니다.
- **확률 기댓값 반올림**: 점수를 기댓값으로 보는 순서형 디코딩.

표본당 순전파가 한 번뿐이라 tau 값을 여러 개 쓸어도 비용이 같습니다.

### 개념 설명 (`test_concept_explain_eval.py`)

서빙에서 어댑터가 실제로 하는 일은 `main.py`의 `_concept_explain()`인데
지금까지 정량 지표가 없었습니다. 어려운 점은 자유 서술이라 정답 문자열이
하나로 정해지지 않는다는 것입니다.

평가셋은 **ConceptMap 149개 항목**을 그대로 씁니다. 사람이 쓴 과목별 핵심
개념 노트이고 서빙에서도 이미 RAG 최상위 레이어로 신뢰하고 있어, 별도
평가셋을 만들 필요가 없습니다.

핵심은 **변별력**입니다. 한국어 설명문은 조사·어미가 겹쳐 아무 말이나 해도
문자 F1이 어느 정도 나옵니다. 그래서 같은 생성문을 (a) 올바른 개념의 참조
(b) 같은 과목의 다른 개념 참조와 각각 비교해 그 **차이**를 봅니다. 차이가
작으면 '그럴듯한 교육용 문장'을 쓰고 있을 뿐 개념을 설명하지 못하는 것입니다.

참조에서 뽑는 용어는 문서빈도가 낮은 것(전체 항목의 8% 이하)만 남겨
조사·상투어를 자동으로 걸러냅니다. 검증: 실제 로그 설명은 커버리지 0.80,
내용 없는 상투어는 0.00.

부수 지표로 한국어 비율, 반복률, `정답:` 형식 오염(수학 어댑터가 문제 풀이
형식으로 새는지), 질문 에코를 같이 잽니다.

> 프롬프트 불일치 주의: 교과 어댑터는 `[학습 지문] / [교육과정 성취기준] /
> 질문:` 형식으로 학습됐는데, `_concept_explain()`은 `질문: '...' 개념의
> 핵심 포인트를...` 만 보냅니다. 학습 분포와 어긋나므로 이 평가 결과는
> 그 영향도 함께 반영합니다.

#### 결과: 개념 설명에서는 어댑터가 베이스보다 못합니다

```
python test_concept_explain_eval.py --n 20 --greedy    # 총 90개 개념
```

커버리지만 보면 길이가 곧 점수가 됩니다. 어댑터 출력이 평균 112자,
베이스가 359자로 3배 차이라 이 지표만으로는 공정하지 않습니다. 그래서
방향을 뒤집은 **밀도**(모델이 꺼낸 개념어 중 참조에 있는 비율)를 같이 재고,
둘의 조화평균인 **개념F1**을 길이 중립 지표로 씁니다.

| 과목 | 커버리지 (A/B) | 밀도 (A/B) | 개념F1 (A/B) |
|---|---|---|---|
| 수학 | 0.189 / **0.281** | **0.274** / 0.265 | 0.180 / **0.261** |
| 국어 | 0.077 / **0.101** | **0.283** / 0.185 | 0.095 / **0.124** |
| 영어 | 0.079 / **0.183** | **0.302** / 0.237 | 0.108 / **0.203** |
| 과학 | 0.140 / **0.158** | 0.177 / **0.236** | 0.117 / **0.183** |
| 사회 | 0.119 / **0.139** | **0.232** / 0.219 | 0.080 / **0.147** |
| **전체** | 0.123 / **0.178** | **0.254** / 0.231 | 0.119 / **0.189** |

(A=어댑터, B=베이스)

**밀도에서는 어댑터가 4개 과목에서 앞섭니다.** 짧지만 정확하다는 뜻입니다.
그러나 **개념F1은 5개 과목 전부 베이스가 앞섭니다.** 정확함이 3배의
길이 차이를 덮지 못합니다.

교과 어댑터가 교과서 답안 과제에서는 베이스를 크게 앞섰던 것
(char-F1 Δ +0.443)과 정반대입니다. 파인튜닝이 **출력 형식과 답변 톤**을
학습시킨 것이라는 기존 해석과 일관됩니다. 교과서 답안 형식은 그 과제에서
이득이지만, 개념을 폭넓게 설명해야 하는 과제에서는 내용을 좁힙니다.

##### 짝지은 검정 — 과목별로 근거가 다릅니다

같은 개념으로 두 조건을 쟀으므로 평균 비교가 아니라 개념별 차이를 봐야
합니다. 과목당 표본이 14~22개뿐이라 더욱 그렇습니다 (수학 CoT 비교에서
n=100 결론이 n=300에서 뒤집힌 적이 있습니다).

| 과목 | n | 베이스 − 어댑터 | 95% 신뢰구간 | 판정 |
|---|---|---|---|---|
| 영어 | 20 | +0.095 | [+0.043, +0.153] | 베이스 우세 |
| 수학 | 20 | +0.081 | [+0.006, +0.158] | 베이스 우세 |
| 사회 | 14 | +0.068 | [+0.007, +0.133] | 베이스 우세 |
| 과학 | 20 | +0.066 | [+0.017, +0.114] | 베이스 우세 |
| **국어** | 16 | +0.030 | **[−0.017, +0.078]** | **구분 안 됨** |
| 전체 | 90 | +0.070 | [+0.043, +0.097] | 베이스 우세 |

**생성 건전성**

| 지표 | 어댑터 | 베이스 | |
|---|---|---|---|
| 한국어 비율 | **0.965** | 0.885 | 어댑터 우위 |
| 반복률 | **0.027** | 0.101 | 어댑터 우위 |
| `정답:` 형식 오염 | **0.203** | 0.000 | 어댑터 문제 |
| 질문 에코 / 빈 출력 | 0.000 | 0.000 | 둘 다 없음 |

어댑터는 한국어 비율이 높고 반복이 적습니다. 베이스는 영어·마크다운·이모지가
섞이고(`## 📘 1. 지수함수란 무엇인가`) 같은 말을 더 반복합니다.

**형식 오염 0.203은 정확히 수학 15건입니다** (0.203 × 74 = 15.02).
즉 **수학 어댑터는 개념 설명 요청의 100%를 문제풀이 형식으로 답합니다.**

```
질문:   '지수함수' 개념의 핵심 포인트를 학생에게 설명해주세요.
어댑터: 정답: 지수함수는 다음과 같은 형태로 나타냅니다.
        풀이: ① $y=a^{x}$ (단, $a>0$ 이고 $a \neq 1$) ...
```

수학은 커버리지 낙폭도 가장 큽니다(0.185 vs 0.311). 문제 풀이 전용으로
학습된 어댑터를 개념 설명에 쓰는 것이 맞지 않습니다.

> `_math_report()`는 이미 같은 이유로 수학 어댑터를 쓰지 않습니다
> (`counseling.py`: "math 어댑터는 문제 풀이 특화 파인튜닝으로 학습 조언
> 프롬프트에 부적합 — LLM 미사용"). `_concept_explain()`에는 그 판단이
> 반영되지 않았습니다.

#### 반영: 개념 설명은 전 과목 베이스로

`main.py`의 `_concept_explain()`에서 모든 과목이 어댑터를 끄고 베이스로
생성합니다(`model.disable_adapter()`). 시스템 프롬프트는 과목별로 그대로
둡니다 — 위 표의 베이스 수치가 바로 그 조합으로 측정된 값입니다.

국어는 개별 신뢰구간이 0을 포함하지만 베이스를 씁니다. **n=16에서 +0.030을
못 잡는 것은 검정력 부족이지 효과가 없다는 증거가 아닙니다.** 국어 구간
[−0.017, +0.078]은 전체 구간 [+0.043, +0.097]과 크게 겹쳐 국어가 다르게
움직인다는 근거가 없고, 부트스트랩 재표집의 88.7%에서 베이스가 앞섰습니다.
검정력이 모자란 하위집단만 따로 떼어내는 것은 옳지 않습니다.

> **판단을 두 번 고쳤습니다.**
> 처음에는 커버리지만 보고 수학·영어만 전환했습니다. 길이 보정을 넣자
> 순서가 바뀌어(사회 1.84배 > 수학 1.45배) 기준이 일관되지 않게 됐고,
> 짝지은 검정으로 과학·사회를 추가했습니다. 그때 국어는 "개별로 유의하지
> 않으니 유지"로 뒀는데, 그 타이브레이크로 쓴 한국어 비율·반복률이
> **전 과목 평균이라 국어 고유 근거가 아니었습니다.** 검정력 논리와 함께
> 다시 보고 국어도 전환했습니다.

##### 학습 형식 프롬프트로 회복되는가 — 아니오, 더 나빠집니다

교과 어댑터는 `[학습 지문] / [교육과정 성취기준] / 질문:` 형식으로 학습됐는데
`_concept_explain()`은 질문만 보냅니다. 이 불일치가 원인일까 싶어 재봤습니다.

설계에서 두 가지를 막았습니다.

- **정답 누설**: 이 평가의 참조가 ConceptMap인데 그게 RAG 최상위 레이어라,
  검색 결과를 지문으로 넣으면 정답이 프롬프트로 들어갑니다. FAISS에서
  뽑되 ConceptMap으로 적재한 문서는 제외했습니다(72개 개념 전부 확보).
- **불공정 비교**: 지문을 어댑터에만 주면 어댑터 효과인지 지문 효과인지
  갈리지 않습니다. **양쪽 모두** 같은 지문·같은 형식으로 줬습니다.

| 과목 | 질문만 (기존) | 학습 형식 + 지문 |
|---|---|---|
| 국어 | +0.030 (구분 안 됨) | **+0.120** |
| 과학 | +0.066 | +0.082 |
| 사회 | +0.068 | +0.096 |
| 영어 | +0.095 | +0.124 |
| **전체 (n=70)** | +0.070 | **+0.105** |

(베이스 − 어댑터 개념F1. 클수록 베이스가 낫다는 뜻)

**격차가 오히려 벌어졌고 4과목 모두 유의해졌습니다.** 어댑터 출력은
112자 → **68자**로 더 짧아졌습니다. 학습 형식을 주니 학습한 대로 더 간결한
교과서 답안을 내놓는 것입니다. 개념 설명은 이 어댑터들이 학습한 과제가
아니며, 프롬프트를 맞춘다고 되는 일이 아닙니다.

##### 조치: 교과 어댑터 4개를 로드하지 않습니다

```python
LOAD_SUBJECT_ADAPTERS = False   # main.py
```

쓰이던 곳이 `_concept_explain()` 하나뿐이었고 거기서 베이스로 돌렸으므로,
올려두기만 하고 호출되지 않는 상태였습니다. 개당 167MB, **약 668MB의
VRAM과 기동 시간**을 아낍니다. 어댑터 파일은 `train/`에 그대로 있어
위 값을 `True`로 바꾸면 되돌릴 수 있습니다.

`math`와 `writing`은 유지합니다 — `writing`은 피드백 생성과 `llm_score`에
실제로 쓰이고, `math`는 `PeftModel`을 만드는 첫 어댑터라 구조상 필요합니다.

**영어는 다시 재서 베이스로 전환했습니다.** 전체 20개 개념, greedy 디코딩:

| 지표 | 어댑터 | 베이스 |
|---|---|---|
| 커버리지 | 0.079 | **0.183** (2.3배) |
| 변별력 Δ | 0.064 | **0.116** |
| char-F1 | 0.162 | **0.295** |
| 한국어 비율 | 0.981 | 0.711 |

전 과목 중 격차가 가장 큽니다. 원인은 **학습 답안 길이**입니다.

| 과목 | 학습 답안 중앙값 | 참조 중앙값 | 비율 |
|---|---|---|---|
| **영어** | **20자** | 121자 | **0.17** |
| 국어 | 35자 | 102자 | 0.34 |
| 사회 | 39자 | 86자 | 0.45 |
| 과학 | 44자 | 94자 | 0.47 |

이 비율 순서가 커버리지 격차 순서와 그대로 일치합니다. 영어 어댑터는
20자짜리 교과서 답안으로 학습돼 한 문장만 내놓습니다. 실제로 '수동태'를
물으면 `be + 과거분사`조차 언급하지 않습니다. 지식 부족이 아니라
**과제 불일치**입니다.

베이스의 한국어 비율 0.711은 낮지만, 영어 과목 설명에 `Passive Voice`
같은 영어 용어가 섞이는 것은 결함이 아닙니다.

> 이 표는 **커버리지 지표 자체의 한계**도 보여줍니다. 짧지만 옳은 답이
> 낮게 나옵니다. 그래서 평가에 평균 출력 길이를 같이 찍도록 했습니다 —
> 커버리지가 낮은 게 "내용이 틀려서"인지 "짧아서"인지 구분하려면
> 길이를 봐야 합니다.

**부작용 처리 — 마크다운 정리.** 수학을 베이스로 돌리자 학생에게 보이는
출력에 마크다운 헤더와 이모지가 섞여 나왔습니다(`## 📌 1. **정의**`).
어댑터는 그런 걸 쓰지 않아 지금까지 드러나지 않던 문제입니다.
`_strip_markdown()`으로 헤더 기호·굵게 표시·수평선·이모지를 걷어냅니다
(`writing.py`의 `_llm_feedback()`도 같은 이유로 같은 처리를 합니다).

굵게 표시는 `**쌍**`으로만 잡습니다. 홑별표까지 지우면 **수학 설명의
곱셈 기호가 사라집니다**(`2*3=6` → `23=6`). 수학을 베이스로 돌리는
이상 실제로 발생하는 문제라 이 구분이 필요합니다. LaTeX(`$y=a^{x}$`)와
어댑터의 평문 출력은 손대지 않습니다.

### 수학 어댑터

`test_math_adapter_eval.py`로 측정합니다. 수학 정답은 `정답: ③ 50` 처럼 짧고
형식이 정해져 있어서 char-F1을 주지표로 쓰면 풀이가 길수록 점수가 오르는
엉뚱한 신호가 나옵니다. `정답:` 줄만 뽑아 LaTeX·공백을 정규화해 비교합니다.

```
python test_math_adapter_eval.py --n 100
python test_math_adapter_eval.py --n 100 --text-only   # 그림·표 참조 문항 제외
```

**결과** (각 100건)

| 지표 | 어댑터 | 베이스 | 어댑터(텍스트 전용) |
|---|---|---|---|
| 정답 일치 | 10.0% | 0.0% | 11.0% |
| 객관식 일치 | **35.2%** (n=71) | 0.0% | **36.4%** (n=66) |
| char-F1 (정답줄) | 0.555 | 0.020 | 0.609 |
| `정답:` 형식 준수 | **100%** | 4.0% | 100% |

**형식은 완벽히 학습했지만 정답률은 낮습니다.** 객관식 정답률이 36% 수준인데,
이건 균등 무작위(20%)와 비교할 수치가 아닙니다. 정답 분포가 1번 쪽으로
치우쳐 있어 **항상 1번만 찍어도 30.4%**입니다(아래 "오류 분석" 참고). 그
현실적 기준선 위의 초과분은 6%p 남짓이고, 교과 어댑터(char-F1 0.69~0.84)에
비하면 확연히 떨어집니다. 오답 사례 중에는 형식은 맞고 값만 틀리는 것이 있습니다:

```
정답   : ③ $40^{\circ}$
어댑터 : ③ $70^{\circ}$
```

두 가지를 짚어둘 필요가 있습니다.

**베이스의 0%는 실력이 아니라 형식 문제입니다.** 베이스는 `정답:` 형식을 4~8%만
지켜서("주어진 문제는 **이등변삼각형**의 성질과…" 식으로 서술) 답 추출 자체가
되지 않습니다. 따라서 베이스와의 정답률 비교는 의미가 약하고, 실질적인 기준은
**객관식 36% vs 항상 1번 30.4%** 쪽입니다(균등 무작위 20%가 아닙니다).

**그림 문항 때문이 아닙니다.** eval의 28%가 `다음 그림에서…`처럼 텍스트 전용
모델이 구조적으로 못 푸는 문항이라 이를 제외하고 재측정했지만, 정답률은
10.0%→11.0%, 객관식은 35.2%→36.4%로 거의 변하지 않았습니다. 평가셋이 불리해서가
아니라 실제 수리 추론 능력이 부족한 것입니다.

> 다만 이 평가는 **문제 풀이** 능력을 잰 것이고, 현재 서빙에서 수학 어댑터가
> 실제로 하는 일은 `_concept_explain()`의 **개념 설명**입니다. 둘은 다른
> 과제이므로 이 수치를 곧바로 "서빙 품질이 나쁘다"로 읽으면 안 됩니다.

#### 원인: 학습 데이터가 정답을 풀이보다 먼저 쓴다

학습 데이터를 뜯어보니 원인이 데이터 순서에 있었습니다.

```
정답: ② $27\sqrt{3}\,\mathrm{cm}^2$      ← 답이 먼저
풀이: AG:GD = 2:1 이므로 ...             ← 추론은 그 다음
```

`math_train.jsonl` 12,640건 중 **"풀이 → 정답" 순서는 0건**입니다.
77.2%가 정답을 먼저 쓰고 풀이를 뒤에 붙이며, 나머지 22.8%는 풀이가 없습니다.

이 순서로 학습하면 모델은 답 토큰을 **중간 계산 없이 한 번의 forward pass로**
내놓도록 배웁니다. 뒤에 붙는 풀이는 이미 확정된 답을 사후 합리화할 뿐이라
정답률에 기여하지 못합니다. 관측된 증상(형식 준수 100%, 정답률 낮음,
형식은 맞고 값만 틀림)이 정확히 이 구조에서 나옵니다.

원인은 `train/preprocess.py`입니다.

```python
output_parts = [f"정답: {answer}"]
if explanation:
    output_parts.append(f"풀이: {explanation}")
```

#### 대응: CoT 순서로 재학습

`train/preprocess_math_cot.py`가 순서를 뒤집어 `풀이 → 정답`으로 재배열합니다.
데이터를 새로 구할 필요 없이 기존 건수가 그대로 CoT 학습 데이터가 됩니다.

- 학습 9,735건 / 평가 1,220건 (풀이 없는 건 제외 — 이런 샘플은 "바로 답부터
  쓰기"를 다시 가르쳐 CoT 신호를 흐립니다)
- `MAX_LENGTH` 640 → 768. 정답이 시퀀스 맨 뒤로 가서, 잘리면 풀이 꼬리가
  아니라 **정답 자체가** 날아갑니다. 그래서 전처리에서 길이 초과분을
  자르지 않고 아예 제외합니다(학습 16건, 평가 8건).
- 학습: `train/train_math_lora_qwen_cot.py` → `train/math_adapter_qwen_cot/`
  (하이퍼파라미터는 기존과 동일, 데이터 순서만 다름)
- best eval_loss **0.4837** (step 600/1216)

> eval_loss를 기존 어댑터(0.4381)와 비교하면 안 됩니다. 긴 풀이를 먼저
> 생성하는 과제로 바뀌어 손실 자체가 다른 값을 잽니다. 판단은 정답률로 합니다.

평가 스크립트는 `정답:` 줄을 정규식으로 찾으므로 순서가 바뀌어도 그대로
쓸 수 있고, 기존 어댑터와 같은 표본으로 비교됩니다.

#### 결과: 측정 가능한 차이가 없습니다

```
python test_math_adapter_eval.py --n 300 --adapter train/math_adapter_qwen \
    --max-new-tokens 512 --dump train/dump_orig.jsonl
python test_math_adapter_eval.py --n 300 --adapter train/math_adapter_qwen_cot \
    --max-new-tokens 512 --skip-base --dump train/dump_cot.jsonl
python compare_math_dumps.py train/dump_orig.jsonl train/dump_cot.jsonl
```

같은 300문항(객관식 204)을 두 어댑터로 풀렸습니다. **같은 문항으로 잰
짝지은 표본**이므로 비율을 나란히 놓고 보는 대신 McNemar 정확검정을 씁니다 —
둘 다 맞힌 문항과 둘 다 틀린 문항은 우열에 대한 정보를 주지 않고,
판단에 쓰이는 건 한쪽만 맞힌 불일치 쌍뿐입니다.

| 지표 | 기존 | CoT | 불일치 쌍 | p | 판정 |
|---|---|---|---|---|---|
| 객관식 일치 (보기번호) | 35.3% | 31.9% | 34 vs 27 | 0.443 | 차이 없음 |
| 값만 일치 (번호 무시) | 23.5% | 27.0% | 9 vs 16 | 0.230 | 차이 없음 |
| 정답 완전일치 | 12.7% | 13.0% | 18 vs 19 | 1.000 | 차이 없음 |

**세 지표 모두 우연과 구분되지 않습니다.** `풀이 → 정답` 재배열은 좋아지지도
나빠지지도 않았습니다.

> **n=100에서 내렸던 결론 두 개가 모두 표본 탓이었습니다.**
> "객관식 8.4%p 악화"는 n=300에서 3.4%p(p=0.44)로 줄었고,
> "CoT가 값을 더 맞힌다"도 3.5%p(p=0.23)에 그쳤습니다. 71문항에서
> 6건 차이를 보고 방향을 말한 것이 성급했습니다.
> `--dump`과 `compare_math_dumps.py`를 만든 이유가 이것입니다.

서빙 어댑터(`src/api/adapters.py`의 `MATH_ADAPTER_PATH`)는 그대로 둡니다.
바꿀 근거가 없습니다.

#### 남는 관찰

- **불일치가 큽니다.** 객관식 204문항 중 61문항(30%)에서 두 어댑터의
  정오가 갈립니다. 같은 데이터·같은 하이퍼파라미터로 학습했는데도
  이렇습니다. 둘 다 맞힌 것은 38문항(18.6%)뿐입니다. 5지선다 우연
  기대값이 20%라는 점과 함께 보면, 맞힌 문항의 상당수가 실력이 아닐
  수 있습니다.
- **값은 맞고 번호만 틀리는 경우**가 기존 25건(12.3%), CoT 32건(15.7%)
  있습니다. 다만 다른 지표가 모두 유의하지 않으므로 이 차이도 잡음으로
  봐야 합니다.
- 기존 어댑터의 객관식 35.3%가 값 일치 23.5%보다 높습니다. 번호는 맞고
  값 문자열이 다른 경우에는 표기 차이(LaTeX·단위)와 함께 계산 없이 찍어
  맞은 경우가 섞여 있습니다.

**다음 시도는 순서 재배열이 아니어야 합니다.** 이 데이터로 이 하이퍼파라미터
아래에서는 출력 순서가 지렛대가 아니라는 것이 확인됐습니다.

#### 오류 분석 — 채점 규칙과 기준선을 바로잡습니다

저장해 둔 문항별 덤프(600건)로 GPU 없이 분석했습니다.

**(1) 엄격한 채점이 CoT를 실제보다 낮게 셌습니다.** 객관식 204건 중
보기 번호를 추출하지 못한 답(`None`)이 기존 28건, CoT 38건입니다. 모델이
`① 8/45` 대신 `8/45`처럼 **값만 쓴 경우**입니다. 이 중 값이 맞은 것이
CoT는 10건, 기존은 3건이라, 번호를 요구하는 채점은 CoT를 불리하게 셉니다.

| 채점 | 기존 | CoT | 불일치 쌍 | McNemar p |
|---|---|---|---|---|
| 엄격 (보기번호 필수) | 35.3% | 31.9% | 34 vs 27 | 0.443 |
| **관대 (번호 없이 값만 맞아도 정답)** | **36.8%** | **36.8%** | **32 vs 32** | **1.000** |

관대한 채점에서는 두 어댑터가 **정확히 같습니다**(75/204). 앞서 본 3.4%p 격차는
CoT가 보기 번호를 덜 쓰는 습관에서 나온 것이었고 실력 차이가 아니었습니다.

**(2) "무작위 20%"는 너무 낮은 기준선이었습니다.** 정답 분포가 치우쳐 있어
(1번 62건, 2번 53건, 3번 42건, 4번 25건, 5번 22건) 항상 1번만 찍어도
**30.4%**입니다.

| 기준선 | 정확도 | 어댑터(36.8%) 대비 단측 p |
|---|---|---|
| 균등 무작위 | 20.0% | < 0.0001 |
| 정답 분포 편향 찍기 | 22.9% | < 0.0001 |
| **항상 1번** | **30.4%** | **0.030** |

모델이 신호를 담고 있는 것은 확실하지만, 현실적인 기준선(30.4%) 위의 초과분은
**6%p 남짓**입니다. "객관식 36%, 무작위 20%"라는 서술은 실력을 과대평가하게
만듭니다.

**(3) CoT는 1번 쏠림이 심합니다.** 예측 분포가 1번 87건(42.6%)으로, 정답 분포의
1번 비율(30.4%)보다 훨씬 높습니다. 기존은 62건(30.4%)으로 정답 분포와
비슷합니다.

왜 안 통했는지에 대한 추정입니다.

- 이 데이터의 "풀이"가 추론 과정으로 기능하지 못했을 가능성이 있습니다.
  다만 **측정해 보니 단언할 수는 없습니다** — 풀이의 약 40%는 등호 3개 이상의
  계산 연쇄이고 약 35%는 60자 미만의 한 문장 설명입니다(아래 "수학 학습 데이터
  후보" 절 참고).
- 답을 내기 전에 200~400토큰을 생성해야 하므로 **중간에서 어긋나면 그대로
  오답으로 이어집니다**. 답을 먼저 쓰던 구조에는 없던 실패 경로입니다.
- 풀이 없는 2,889건을 빼면서 학습량이 12,640 → 9,735건으로 줄었습니다.

왜 아무 차이도 안 났는지에 대한 추정입니다.

- 풀이가 답을 유도하는 연산으로 기능하지 못했다면, 순서를 어느 쪽으로 놓든
  답은 결국 한 번의 forward pass에서 나오는 셈이라 차이가 없습니다. 다만
  이 데이터의 풀이는 **혼합**입니다. 등호 3개 이상의 계산 연쇄가 약 40%,
  등호가 하나도 없는 설명이 약 41%입니다. 계산 연쇄가 40%나 들어 있는데도
  차이가 없었으므로 "요약이라서 안 통했다"는 설명은 절반만 맞습니다.
- 풀이 없는 2,889건을 빼면서 학습량이 12,640 → 9,735건으로 줄었습니다.
  CoT의 이득이 있었더라도 이 손실과 상쇄됐을 수 있습니다.

#### 평가의 근본 결함 — 객관식 문항에 보기가 없다

베이스의 추론 모드를 재려다 발견했습니다. 모델이 계산은 맞게 하고도 "보기가
주어지지 않아 번호를 정할 수 없다"며 한참 고민하다 엉뚱한 번호를 찍었습니다
(`8/45`를 구해 놓고 정답 `① 8/45` 대신 `④`를 선택).

확인해 보니 **평가셋 객관식 204문항 전부에서 문제 본문에 보기가 없습니다.**
71859의 `문항(텍스트)`에는 보기가 들어 있지 않고(보기는 이미지에만 있음),
정답 텍스트에만 `③ 40°`처럼 번호가 붙어 있습니다. 그림·표를 참조하는 문항도
37.3%(76건)입니다.

즉 지금까지 "객관식 정답률"이라 부른 수치는 **보이지 않는 보기의 순서를 추측한
결과**입니다. 번호는 문제 내용과 무관한 순서 정보라 실력을 재지 못합니다.
앞선 관찰들이 이걸로 설명됩니다.

- "값은 맞고 번호만 틀린" 사례가 계속 나온 이유
- 정답이 1번에 치우치면(30.4%) 항상 1번만 찍어도 그만큼 나오는 이유
- CoT가 번호를 덜 쓰고 값만 쓰는 쪽으로 간 이유

**따라서 수학 정확도는 번호가 아니라 값 일치로 봐야 합니다.** 값 일치는 기존
어댑터 23.5%, CoT 27.0%이고 두 값의 차이는 유의하지 않았습니다(p=0.230).
위 "객관식 36.8%, 기준선 30.4%" 논의는 번호 정확도에 대한 것이라 실력의
근거로 쓸 수 없습니다. 값은 71859 객관식 정답의 대부분에서 신뢰할 만합니다
(번호를 뗀 값이 비어 있는 것이 1건, 중앙값 6자). 다만 서술형 보기는 값 비교가
정확하지 않을 수 있습니다.

#### 값 일치 채점 (`math_value_match.py`)

단순 문자열 비교는 같은 값을 다르게 쓴 답을 오답으로 처리합니다. 실제 사례:
`12` 대 `12명`(단위), `1.44` 대 `1. 44`(공백), `x^2-8x+25` 대 `x^{2}-8x+25=0`
(표기와 `=0`), `8/45` 대 분수 표기. 이런 차이를 흡수하되 과하게 후하지 않도록,
서로 다른 문항의 답과 정답을 섞어 짝지어 **우연 일치율이 0.42%** 임을 확인했습니다.

기존 `strip_choice`는 모델 답에 쓰면 안 됩니다. `1.44`처럼 숫자와 점으로 시작하는
값의 앞 `1.`을 보기 번호로 보고 지워 `44`로 만듭니다. 같은 함수로 어댑터 덤프를
다시 채점한 값이 이후 비교의 기준선입니다.

| 어댑터 (같은 204문항) | 이전 값 일치 | 개선된 채점 |
|---|---|---|
| 기존 (answer-first) | 23.5% | **25.5%** (52/204) |
| CoT (풀이-first) | 27.0% | **28.9%** (59/204) |

두 어댑터의 차이는 여전히 유의하지 않습니다(불일치 10 대 17, p=0.248).

#### 베이스 Qwen3-8B 추론 모드가 미세조정 어댑터를 앞선다

```
python test_math_base_thinking.py --mode think --max-new-tokens 3072 --dump train/dump_think.jsonl
```

미세조정 어댑터는 `enable_thinking=False`로만 써 왔고, 베이스의 추론 모드
(`enable_thinking=True`)는 재본 적이 없었습니다. 같은 204문항, 같은 값 일치
채점으로 쟀습니다. 생각이 3,072토큰 안에 안 끝난 문항은 `</think>`를 붙여 그때까지의
생각으로 답을 내게 했고(budget forcing) 따로 셌습니다.

| | 전체 (204) | 그림·표 참조 (76) | 그림 없음 (128) |
|---|---|---|---|
| 기존 어댑터 | 25.5% | 6.6% | 36.7% |
| CoT 어댑터 | 28.9% | 7.9% | 41.4% |
| **베이스 추론 모드** | **35.8%** (73/204) | 13.2% | **49.2%** (63/128) |

| 짝지은 비교 (McNemar) | 불일치 쌍 | p |
|---|---|---|
| 전체: 추론 모드 대 기존 | 25 대 4 | **0.0001** |
| 전체: 추론 모드 대 CoT | 20 대 6 | **0.0094** |
| 그림 없음: 추론 모드 대 기존 | 19 대 3 | 0.0009 |
| 그림 없음: 추론 모드 대 CoT | 14 대 4 | 0.0309 |

베이스 추론 모드가 두 어댑터를 모두 유의하게 앞섭니다. 그림 참조 문항을 빼도
같습니다.

- 자연 종료 173건은 38.7%, 예산 강제 종료 31건은 19.4%입니다. 강제 종료
  31건 중 **25건이 그림 참조 문항**입니다. 그림이 없어 풀 수 없는 문제에서
  끝없이 고민한 것이라, 강제 종료분의 낮은 정답률은 추론 능력이 아니라 문제
  결함을 반영합니다.
- 생성 토큰은 평균 1,370 / 중앙값 1,039 / 최대 3,072입니다. 어댑터보다 훨씬
  깁니다(문항당 약 34초, 어댑터는 수 초).

**이 비교에는 교란 요인이 하나 있었습니다.** 추론 모드는 "보기가 주어지지
않으니 값만 쓰라"는 답 형식 지시를 받았고 어댑터는 받지 않았습니다. 그래서
추론을 끈 베이스를 **같은 지시**로 따로 쟀습니다.

```
python test_math_base_thinking.py --mode nothink --max-new-tokens 1024 --dump train/dump_nothink.jsonl
```

| 조건 (같은 204문항, 같은 채점) | 전체 | 그림 없음 (128) |
|---|---|---|
| 기존 어댑터 | 25.5% (52) | 36.7% |
| CoT 어댑터 | 28.9% (59) | 41.4% |
| **베이스, 추론 끔 + 같은 지시** | **28.9%** (59) | 43.0% |
| **베이스, 추론 켬** | **35.8%** (73) | **49.2%** |

| 짝지은 비교 | 불일치 쌍 | p |
|---|---|---|
| 베이스 추론 켬 대 끔 (전체) | **15 대 1** | **0.0005** |
| 베이스 추론 켬 대 끔 (그림 없음) | 9 대 1 | 0.0215 |
| 베이스 끔 대 기존 어댑터 | 18 대 11 | 0.265 |
| 베이스 끔 대 CoT 어댑터 | **14 대 14** | 1.000 |

두 가지가 분리됩니다.

1. **이득의 원천은 추론 모드 자체입니다.** 같은 지시를 받은 추론 끔(28.9%)과 비교해도
   35.8%로 유의하게 높습니다. 형식 지시 때문이 아닙니다.
2. **71859 미세조정은 베이스 위에 아무것도 더하지 못했습니다.** 추론을 끈 베이스가
   CoT 어댑터와 14 대 14로 똑같고, 기존 어댑터보다도 점추정은 높습니다. "미세조정이
   성능을 깎았다"는 증거는 없고 "더하지 못했다"가 정확한 표현입니다. 앞서 세운
   "짧은 풀이 데이터가 베이스의 추론 능력을 깎는다"는 가설은 지지되지 않았습니다.

**추론 모드의 비용:** 문항당 약 34초(어댑터·추론 끔은 5.5초)입니다. 생성 토큰이
평균 1,370개로 추론 끔(평균 279개)의 5배입니다.

#### 71718 학습 결과 — 풀이를 3배 늘려도 바뀌지 않았다

```
python train/train_math_lora_qwen_718.py     # 하이퍼파라미터는 CoT와 동일, 데이터만 다름
python test_math_adapter_eval.py --n 300 --adapter train/math_adapter_qwen_718 \
    --max-new-tokens 512 --skip-base --dump train/dump_718.jsonl
```

71718 데이터(중학교 1학년~고등학교 공통수학, 10,000건)로 재학습했습니다. 이
데이터는 풀이가 훨씬 풍부합니다(등호 3개 이상 73.8% 대 기존 39.5%, 풀이 중앙값
207자 대 93자). best eval_loss 0.3061.

같은 204문항, 같은 값 일치 채점(McNemar)입니다.

| 조건 | 정확도 | 그림 있음 (76) | 그림 없음 (128) |
|---|---|---|---|
| 기존 어댑터 | 25.5% | 6.6% | 36.7% |
| CoT 어댑터 | 28.9% | 7.9% | 41.4% |
| **71718 어댑터** | **27.5%** (56) | 5.3% | 40.6% |
| 베이스, 추론 끔 | 28.9% | 5.3% | 43.0% |
| 베이스, 추론 켬 | 35.8% | 13.2% | 49.2% |

| 짝지은 비교 | 불일치 쌍 | p |
|---|---|---|
| 71718 대 기존 어댑터 | 16 대 12 | 0.572 |
| 71718 대 CoT 어댑터 | 11 대 14 | 0.690 |
| 71718 대 베이스 끔 | 12 대 15 | 0.701 |
| **71718 대 베이스 추론** | 5 대 22 | **0.0015** |

**71718은 다른 어댑터·베이스끔 어느 것과도 유의한 차이가 없고, 베이스 추론
모드에는 여전히 유의하게 뒤집니다.** 풀이 데이터를 3배로 늘리고 계산 과정을
훨씬 풍부하게 만들어도 결과가 바뀌지 않았습니다. 답 형식은 의도대로
작동했습니다(`정답:` 형식 준수 99.7%, 답이 보기 번호로 시작하는 경우 2.5%뿐 —
번호가 아니라 값을 쓰도록 학습한 목표를 달성).

**결론: 이 파이프라인(짧거나 중간 길이의 풀이 텍스트로 LoRA 미세조정, 추론
비활성)은 데이터 종류를 바꿔도 성능 상한이 25~29%대에 갇힙니다.** 상한을
깨는 유일한 조건은 베이스의 자체 추론 모드였습니다(35.8%, 6배 느림). 데이터
확보 방향의 개선은 여기서 막다른 길입니다. 다음 단계가 있다면 데이터가 아니라
**추론 모드를 실제로 쓸 수 있는 형태**(경량화, 캐싱, 비동기 처리 등)여야
합니다 — 단, 아래에서 보듯 그럴 소비처 자체가 현재 없습니다.

#### 서빙에는 수학 풀이의 소비처가 없습니다

수학 정확도를 올리려던 작업의 소비처를 확인해 보니 **서빙에 수학 문제를 푸는
엔드포인트가 없습니다.**

- 수학 개념 설명은 이미 어댑터를 끄고 베이스로 씁니다(`main.py`의 `_concept_explain`).
- `math` 어댑터를 요청 경로에서 켜는 곳이 없습니다. 기동 시 기본 어댑터 지정 한 줄이
  전부입니다. 교과 어댑터 4개와 같은 상태(로드만 되고 호출되지 않음)입니다.
- `/report/math`는 규칙 기반 텍스트를 조립할 뿐 LLM을 쓰지 않습니다.

즉 수학 풀이 정확도는 **현재 제품의 어떤 기능도 지탱하지 않습니다.** 이 정확도가
필요해지는 것은 "문제를 자동으로 풀어 정오답을 판정하거나 풀이를 생성하는" 기능을
새로 만들 때입니다.

**코드베이스를 조사한 결과, 그런 계획의 흔적은 없습니다.**

- `ocr/grading.py`(라즈베리파이 워크시트 채점)에 이미 "정오답 판정" 기능이 있지만,
  LLM이 문제를 푸는 방식이 아닙니다. 호출자가 `questions.json`에 정답(`expected`)을
  **미리 정의해서 넘겨야** 하고(모듈 주석: "이 모듈은 어디에 답이 있는지 찾는 검출은
  하지 않는다"), OCR로 읽은 손글씨와 편집거리로 비교할 뿐입니다. 수학 LLM의 풀이
  능력이 필요하지 않습니다.
- `java_client.py`의 백엔드 연동은 `/api/wrong-answer/ai-pipeline` 하나뿐이고,
  이미 발생한 오답을 조회해 약점을 분석하는 용도입니다. 새로 문제를 풀거나
  채점을 요청하는 API가 아닙니다.
- 전체 API 스키마(`writing.py`의 요청/응답 클래스들)에 수학 풀이·자동 채점을 위한
  것이 없고, `TODO`·로드맵·예정 표시도 찾지 못했습니다.

코드베이스에 흔적이 없다는 것이 계획이 없다는 증명은 아닙니다(문서화되지 않은
백엔드 쪽 계획일 수 있습니다). 다만 이 리포지토리 안에서는 소비처가 없다는 것은
확인됐으므로, **이 조사를 근거로 수학 어댑터에 대한 추가 데이터 확보나 반복
실험은 여기서 멈춥니다.** 71718 학습·평가(진행 중이던 것)는 "미세조정이 베이스에
무언가를 더하는가"에 대한 유효한 지식이라 끝까지 마치지만, 그 결과가 무엇이든
서빙 어댑터는 교체하지 않습니다.

---

## 한국사만 다른 이유

한국사는 **어댑터도 RAG 코퍼스도 없습니다.**

- AI-Hub 교육과정 데이터에 한국사 과목이 없어 어댑터를 학습하지 못했습니다
  (`train/preprocess_curriculum.py`는 국어/영어/과학/사회 4과목만 대상).
- FAISS 코퍼스 698,347건 중 한국사 문서는 **13건**뿐입니다. 참고로 다른
  과목은 7.6만~18.7만 건입니다.

문서가 이렇게 적으면 과목 필터가 사실상 무력화돼, 키워드 필터("조선", "고려",
"신라"…)에 아무 문서나 걸립니다. 실제로 "신간회" 검색에 발해 역사책 설명이,
"갑오개혁"에 동학농민운동 폐정개혁안이 반환되고 있었습니다.

그래서 한국사는 한동안 `_FAISS_EXCLUDED_SUBJECTS`로 지정해 FAISS를
건너뛰고 ConceptMap만 썼습니다. **지금은 해제했습니다** — ConceptMap 42개
항목을 청크로 나눠 FAISS에 적재해 98건이 됐고, 과목 한정 검색이 들어가면서
적중률이 33% → 81%로 뒤집혔습니다. 위 "RAG 개선" 절 참고.
개념 설명은 Ollama(`llama3.1:latest`)로 폴백합니다. 베이스 Qwen으로 바꾸는 것을
검토했지만 하지 않았습니다(아래 절).

### 한국사 개념 설명: 베이스 Qwen으로 바꾸지 않은 이유 (2026-09-23)

```
python test_korean_history_explain_eval.py      # 42개 전부, greedy
```

다른 과목은 전부 베이스 Qwen으로 개념 설명을 만드는데 한국사만
`_concept_explain()`이 `None`을 돌려 Ollama로 넘어가고 있었습니다. 한국사
ConceptMap 42개 개념으로, 지금 서빙 중인 Ollama(두 경로의 실제 폴백 프롬프트,
`num_predict=150`, 타임아웃 20초)와 베이스 Qwen(`_concept_explain` 프롬프트)을
짝지어 쟀습니다. 채점은 `test_concept_explain_eval.py`와 같습니다.

| 조건 | 커버리지 | 밀도 | 개념F1 | 반복률 | 길이 | 소요 중앙값 |
|---|---|---|---|---|---|---|
| **베이스 Qwen** | **0.061** | 0.182 | **0.086** | **0.090** | 321자 | 9.8초 |
| Ollama (`subject-recommend` 프롬프트) | 0.054 | **0.200** | 0.075 | 0.203 | 235자 | 3.3초 |
| Ollama (`/report/{subject}` 프롬프트) | 0.025 | 0.150 | 0.038 | 0.179 | 231자 | 3.2초 |

| 짝지은 비교 (개념F1, Qwen − Ollama) | 평균차 | 95% 신뢰구간 | 판정 |
|---|---|---|---|
| 대 `subject-recommend` | +0.011 | [−0.010, +0.031] | 구분 안 됨 (Qwen 우세 84%) |
| 대 `/report/{subject}` | +0.048 | [+0.034, +0.062] | Qwen 우세 |

개념F1로만 보면 Qwen이 어느 Ollama 경로보다도 못하지 않아서 전환해 서버에서
확인했는데, **신간회를 "조선 후기 사상가 모임"이라고 설명했습니다**(실제로는
1927년 일제강점기의 민족협동전선). 그래서 대표 개념 8개의 핵심 사실을 직접
판정했습니다(`subject-recommend` 경로, 서빙 그대로).

| 개념 | 베이스 Qwen | Ollama |
|---|---|---|
| 신간회 | ✗ 조선 후기 사상가 모임 | ✗ 19세기 말 민족주의 운동 |
| 갑오개혁 | △ 1894년은 맞음, "청일전쟁=한일전쟁" | △ 대략 맞음 |
| 병자호란 | ✗ 지어낸 정의 | ✓ 1636년 청과의 전쟁 |
| 대동법 | ✗ "정치·경제적 이념" | ✗ 고려시대 법률 |
| 4·19 혁명 | ✓ | ✗ "군사 쿠데타" |
| 6월 민주항쟁 | ✗ 1980년 광주 | ✓ 1987년 |
| 서희 | ✗ | ✗ |
| 을사늑약 | ✗ "일본-대만 조약", 영토 편입 | ✓ 1905년 외교권 박탈 |
| **맞은 수** | **약 1.5 / 8** | **약 3.5 / 8** |

**사실 정확도는 Ollama가 낫습니다. 전환하지 않았습니다.**

- **개념F1은 사실 여부를 재지 못합니다.** 참조의 드문 용어가 출력에 나오는지만
  보므로, 길게 쓰며 관련 용어를 많이 꺼내면 틀린 설명도 점수가 오릅니다. Qwen은
  평균 321자로 Ollama(235자)보다 깁니다.
- **두 모델 모두 학생에게 보여주기 어려운 수준입니다.** 8개 중 절반 이상이
  틀립니다. 그리고 같은 리포트 바로 위 "[관련 학습 자료]"에는 ConceptMap의
  정확한 설명이 이미 들어 있습니다. 모델이 그걸 받지 않고 제 지식으로만 쓰기
  때문에, 정답 바로 아래에 틀린 설명이 붙습니다.
- 8개는 작은 표본이지만 오류가 사소한 것이 아니라 사건 자체를 잘못 설명하는
  수준이라 방향은 분명합니다.

> 다른 과목의 사실 정확도는 아래 "개념 설명에 ConceptMap 근거 붙이기" 절에서
> 확인했습니다.

### 개념 설명에 ConceptMap 근거 붙이기 (2026-09-23)

```
python test_grounded_explain_eval.py retrieve train/grounded_ctx.json
python test_grounded_explain_eval.py generate train/grounded_ctx.json train/grounded_out.json
```

같은 리포트에 이미 검색한 자료가 들어가는데 모델은 그걸 받지 않고 제 지식으로만
썼습니다. 그래서 검색 자료를 `[참고 자료]`로 붙이고 "자료와 어긋나는 내용은 쓰지
말 것, 자료에 없는 연도·인물·수치는 확실할 때만, 무관한 자료는 무시할 것"을
지시했습니다(`src/api/grounding.py`).

개념은 Backend 시드(`questions_seed.json`)의 **실제 conceptTag** 23개와 한국사
대표 개념 8개입니다. 개념F1은 사실 오류를 못 잡으므로 점수를 매기지 않고 출력을
나란히 저장해 핵심 사실을 직접 판정했습니다(✓=1, 일부 틀림=0.5, 틀림=0, greedy).

| 과목 | n | Qwen 지금 | Qwen + 근거 |
|---|---|---|---|
| 국어 | 5 | 3 | 3.5 |
| 수학 | 5 | 5 | 5 |
| 영어 | 5 | 2.5 | 2 |
| 과학 | 4 | 4 | 3.5 |
| 사회 | 4 | 4 | 4 |
| **한국사 외 합계** | 23 | **18.5** | **18** |
| 한국사 (Qwen) | 8 | 1.5 | **6** |
| 한국사 (Ollama, 서빙 중) | 8 | 3.5 | **6.5** |

합계만 보면 한국사 외에는 차이가 없는데, **자료 출처로 나누면 방향이 갈립니다.**

| 붙인 자료 (한국사 외) | n | 지금 | + 근거 | 중국어 등 섞임 (지금 → 근거) |
|---|---|---|---|---|
| ConceptMap 항목 | 15 | 12 | **13** | 3 → 3 |
| FAISS 교과서 조각 | 8 | **6.5** | 5 | **0 → 4** |

- **ConceptMap(사람이 쓴 노트)은 도움이 됩니다.** 평시조를 판소리("춘향가")와
  혼동하던 것이 "3장, 종장 첫 음보 3음절"로 바로잡혔고, 한국사는 신간회·대동법·
  서희처럼 사건 자체를 잘못 말하던 것이 대부분 맞게 됐습니다.
- **FAISS 조각은 해롭습니다.** 40~120자짜리 한 문장이라 개념을 정의하지 못하는데
  모델이 거기에 끌려갑니다. "복합관계대명사"에 `what` 설명 조각이 붙자
  whoever/whatever가 아니라 what을 설명했고, "물질 변화"에서는 "나무가 타면
  흑연이 생긴다"가 나왔습니다. 중국어 섞임 증가(0→4건)도 전부 이 경우입니다.

그래서 **ConceptMap에서 온 자료만 근거로 붙입니다**(`grounding.is_curated`). 한국사
FAISS 문서는 ConceptMap을 잘라 적재한 것이라 공백을 빼고 부분 문자열로 비교하면
그대로 걸립니다(31개 개념에서 판별이 모두 맞았습니다). 걸러지고 나면 자료가 없으니
지금 방식과 같아집니다.

적용한 경로:

| 경로 | 생성 | 근거 |
|---|---|---|
| `subject-recommend`, `/report/{과목}` 개념 설명 | Qwen (한국사는 Ollama) | ConceptMap 자료 |
| `/report/math`의 "AI 개념 심층 분석" | Ollama | ConceptMap 자료 |
| `/report/writing`, `/report/premium` | Ollama | 없음 (개념이 아니라 학생 프로필을 받음) |

한국사는 Ollama를 유지합니다. 근거를 붙이면 Qwen 6, Ollama 6.5로 비슷하고, Qwen은
"대한민국의 갑오개혁"처럼 시대가 어긋난 표현을 썼으며, Ollama가 3배 빠릅니다
(1.5초 대 10초). Ollama의 `num_ctx`는 참고 자료가 잘리지 않게 4096으로 올렸고,
Qwen의 입력 한도도 512 → 2048토큰으로 올렸습니다(오른쪽부터 잘려서 한도가 모자라면
질문이 먼저 날아갑니다).

**남은 한계**

- 표본이 작습니다(한국사 외 23개). 한국사 외 ConceptMap 근거의 이득(12→13)은 한
  개념 차이라 "해가 없다" 정도로 읽어야 합니다. 한국사의 이득(1.5→6, 3.5→6.5)은
  분명합니다.
- 근거를 붙여도 틀리는 경우가 있습니다. 병자호란을 Ollama는 "명과 후금의 전쟁"으로
  설명합니다. 자료가 광해군·인조반정부터 시작해 모델이 앞부분에 끌려갑니다.
- **맞는 ConceptMap 항목이 있는데 키워드가 안 맞아 못 찾던 개념이 있었습니다.**
  "현대소설 서술시점"은 `소설의 시점` 항목이, "복합관계대명사"는 whoever가 들어 있는
  `관계부사·복합관계사` 항목이 있었지만 질의어가 키워드와 달라 빗나갔습니다. 키워드
  `서술시점`·`시점`, `복합관계대명사`·`복합관계부사`를 더하자 서술시점은 틀림 → 맞음,
  복합관계대명사는 틀림 → 대체로 맞음(복합관계부사와 섞음)이 됐습니다.
- **정의가 없는 항목은 근거로 주면 해롭습니다.** `수사법` 항목은 "반어·역설"을 이름만
  나열합니다. 여기에 `반어` 키워드를 걸자 모델이 반어를 역설("너무 똑똑해서 아무것도
  모른다")로 설명해 맞음 → 틀림이 됐습니다. 키워드를 되돌렸습니다. 쓰려면 항목에
  정의를 넣어야 합니다.
- ConceptMap에 아예 항목이 없는 개념은 근거 없이 씁니다. 당시 "분사구문 의미상
  주어"가 그랬고, 영어 "관계부사 where/when"은 맞는 항목을 받고도 틀렸습니다("I was
  born in where I live now"). 둘 다 아래 "영어 ConceptMap 보강"에서 다뤘습니다.
- 검색이 완전히 실패하면 `subject_aware_search`가 과목의 앞쪽 ConceptMap 항목을
  돌려줍니다. 이건 ConceptMap 출처라 근거로 붙지만 개념과 무관할 수 있어 "무관하면
  무시하라"는 지시에 기대고 있습니다.
- Qwen 출력에 중국어·키릴 문자가 섞이는 문제는 근거와 무관하게 남아 있습니다
  (근거 없이도 23개 중 3개).

### 영어 ConceptMap 보강 (2026-09-23)

위 평가에서 가장 약했던 영어(5개 중 2~2.5)를 보강했습니다. 영어 항목 20 → 26개.

**기존 항목의 틀린 내용을 고쳤습니다.**

- 관계대명사: "계속적 용법: 앞 절 전체 수식 불가"는 반대입니다. 계속적 용법의
  which는 앞 절 전체를 받습니다(He was late, which made me angry). 계속적 용법에
  못 쓰는 건 that입니다.
- 관계부사: "선행사+관계부사=전치사+관계대명사" → where = in which처럼
  **관계부사 = 전치사+which**가 맞습니다.
- 분사: "excited(흥미로운 感)"의 한자, "주절 주어=분사 의미상 주어"(같을 때만 성립).

**새 항목과 정의 보강:** 분사구문(의미상 주어·독립분사구문·Having p.p.·with 분사구문),
가정법 미래·혼합가정법·가정법 도치, 관계대명사 what, 가주어·가목적어, to부정사·동명사의
의미상 주어. 관계부사와 복합관계사를 나누고 복합관계사에 명사절/양보절 정의를 넣었습니다.
부정사·수동태·현재완료·도치 항목에는 예문을 더했습니다. 항목마다 600자 이내라 근거로
쓸 때 잘리지 않습니다.

**검색 순서도 고쳤습니다.** `search_concept_map`(rag.py에서 `concept_map.py`로 옮김)이
딕셔너리 순서 대신 **더 길게 일치한 항목을 먼저** 돌려줍니다. "to부정사 의미상 주어"에서
"to부정사"로 걸린 부정사 항목이 전용 항목보다 앞에 와서 모델이 명사적 용법을 설명했기
때문입니다. 전 과목 571개 질의(Backend 태그 + 모든 ConceptMap 키워드) 중 16개만 순서가
바뀌었고, 모두 더 구체적인 항목이 앞으로 옵니다(예: "조선 신분제"는 `조선`보다
`조선 신분제` 항목이 먼저). 결과 구성은 같고 순서만 바뀝니다.

**결과** (Backend 영어 태그 10개 + 새로 다룬 개념 2개, greedy, 출력을 읽고 판정)

| 개념 | 근거 없음 | 수정 전 근거 | 수정 후 근거 |
|---|---|---|---|
| 가정법 과거완료 | ✓ | ✓ | ✓ |
| 가정법 미래 | ✗ 1형식 조건문으로 설명 | ✗ "잘 쓰이지 않는 개념" | ✓ should·were to |
| 관계대명사 that/which | ✗ | ✗ | ✗ |
| 관계부사 where/when | ✗ 관계대명사라고 함 | ✗ 비문 예문 | ✓ in which = where |
| 도치구문 강조 용법 | ✗ | ✗ | ✓ |
| 복합관계대명사 | ✗ | △ | ✓ |
| 부정사 명사적 용법 | ✗ 동명사와 혼동 | ✗ 형용사적 예문 | ✓ |
| 분사구문 의미상 주어 | △ | △ (항목 없음) | △ 규칙 하나를 지어냄 |
| 수동태 완료형 | △ | △ | ✓ |
| 현재완료 용법 | ✓ | ✓ | ✓ |
| 관계대명사 what | ✗ | ✗ **"what은 관계대명사가 아니다"** | ✓ |
| to부정사 의미상 주어 | ✗ | ✗ | ✓ for+목적격 |
| **합계** | **3/12** | **3.5/12** | **10.5/12** |

Backend 태그 10개만 보면 3 → 8.5입니다.

- **불완전한 근거는 없느니만 못할 수 있습니다.** 수정 전 관계대명사 항목에는 what이
  없었고, 그 항목을 받은 모델은 "what은 관계대명사가 아니다"라고 단언했습니다.
- **항목 안에서도 첫 문장이 중요합니다.** 가주어와 의미상 주어를 한 항목에 넣었을 때는
  "to부정사 의미상 주어"를 물어도 첫 문장인 가주어 it을 설명했습니다. 둘로 나누자
  맞게 설명했습니다.
- **여전히 틀리는 것:** "관계대명사 that/which"는 항목에 "계속적 용법·전치사 뒤에는
  that 불가"가 있는데도 모델이 "which 뒤에 목적격 대명사가 온다" 같은 말을 지어냅니다.

**평가 재현 주의:** ConceptMap은 `test_concept_explain_eval.py`의 참조 답안이기도
합니다. 영어·국어 항목이 늘고 바뀌어서, 위쪽 "개념 설명" 절의 영어·국어 수치(국어
판정 미확정 포함)는 이제 같은 표본으로 재현되지 않습니다.

### 국어 ConceptMap 정의 보강 (2026-09-23)

영어와 같은 방식으로 국어 항목을 16 → 23개로 늘리고 정의를 채웠습니다.

**기존 항목의 틀린 내용:**

- 음운변동: 구개음화를 자음동화로 분류했습니다. 자음동화는 비음화·유음화이고,
  구개음화는 모음 'ㅣ' 때문에 일어나는 교체입니다. 된소리되기가 빠져 있어 교체·탈락·
  첨가·축약 체계로 다시 쓰고 예시 발음을 붙였습니다.
- 맞춤법: 사이시옷 조건을 "순우리말 합성어"로 적었는데, 두 말 중 하나 이상이 고유어면
  됩니다. "만큼·대로는 띄어 쓴다"는 의존명사일 때만이고, 체언 뒤 조사면 붙입니다.
- 품사: "'-ㄴ다'(동사)/-'다'(형용사)"는 구별법이 되지 않아 현재형·명령형 검사로
  바꿨습니다.

**새 항목:** 고전소설(편집자적 논평·전형적 인물), 고전시가 어휘(괴다·여희다 등 옛말과
소나무·국화·자규 등 상징), 시조(평시조·엇시조·사설시조·연시조), 반어·역설(작품 예시),
독해의 층위(사실적·추론적·비판적 이해), 갈등·구성 단계, 사이시옷. 화자·시점·비문학·
현대소설·수사법 항목에는 정의와 예시를 채웠습니다. `시조`와 `갈등` 키워드는 새 항목으로
옮겨 중복을 없앴습니다.

**결과** (Backend 국어 태그 9개 + 문법 3개, greedy, 출력을 읽고 판정)

| 개념 | 근거 없음 | 수정 전 근거 | 수정 후 근거 |
|---|---|---|---|
| 고전소설 인물 심리 | ✓ | ✓ | ✓ |
| 고전시가 어휘 | ✗ "산조, 현행, 경각시" | ✗ (항목 없음) | ✓ 괴다·여희다 |
| 문학 갈등 구조 | △ | ✓ | ✓ |
| 문학 표현기법 반어 | ✓ | ✓ (항목 없음) | ✓ 「진달래꽃」 |
| 비문학 추론적 이해 | ✓ | ✓ | ✓ |
| 비문학 핵심어 파악 | ✓ | ✓ | ✓ |
| 시조 형식 평시조 | ✗ 춘향가를 시조로 | △ "3·4음보" | ✓ |
| 현대소설 서술시점 | ✗ | ✓ | ✓ |
| 현대시 화자 태도 | △ | ✓ | ✓ |
| 음운 변동 | ✗ | △ 구개음화를 자음동화로 | ✓ |
| 사이시옷 | ✗ | ✗ (항목 없음) | △ 조건은 맞고 예시를 지어냄 |
| 동사와 형용사 구별 | △ 구별법 없음 | △ | ✓ |
| **합계** | **5.5/12** | **8.5/12** | **11.5/12** |

Backend 태그 9개만 보면 5 → 7.5 → 9입니다. 영어(3 → 10.5/12)보다 폭이 작은 것은
베이스 모델이 비문학처럼 일반적인 개념은 원래 설명할 줄 알기 때문입니다. 차이가 난
곳은 고전시가 어휘·시조·음운·서술시점처럼 **교과 고유의 용어와 규칙**이었습니다.

- **수정 전 항목의 틀린 내용이 그대로 옮겨졌습니다.** 수정 전 근거로 "음운 변동"을
  설명하게 하자 모델이 "자음 동화(비음화·유음화·구개음화)"라고 항목의 오류를 그대로
  썼습니다. 근거를 붙이면 항목의 오류도 학생에게 그대로 전달됩니다.
- **한 항목에 여러 주제를 넣으면 옆 주제로 샙니다.** 사이시옷과 띄어쓰기를 한 항목에
  두었을 때 모델이 사이시옷을 설명하다 띄어쓰기로 넘어갔습니다. 사이시옷을 따로
  떼었습니다.
- **용어를 정의하지 않으면 모델이 혼동합니다.** 평시조를 "4음보(즉, 4행)"로 설명해,
  항목에 "음보는 끊어 읽는 마디로 행이 아니다"를 넣자 바로잡혔습니다.

### 한국사 ConceptMap 보강 (2026-09-23)

한국사 항목은 이미 길고 정확했습니다(42개를 모두 대조해 틀린 곳은 두 군데). 문제는
**한 항목에 사건 여러 개가 들어 있고, 개념 이름만 나오고 정의가 없는 것**이었습니다.
신간회는 실력양성운동 항목의 276번째 글자부터 나왔고, 탕평책은 "영조: 탕평책·균역법…"
처럼 이름만 있었습니다. 모델은 이름만 있는 개념을 그럴듯하게 지어냅니다.

**고친 것:**

- 틀린 내용: 고려 향·부곡·소 주민을 천민으로 분류(양민이지만 차별받는 계층), 임시정부
  수립을 1919.9로 표기(4월 상하이 수립, 9월은 3개 정부 통합).
- 정의로 시작하는 전용 항목 7개를 떼어 냄: 탕평책, 삼별초, 훈민정음, 물산장려운동,
  신간회, 광주학생항일운동, 봉오동·청산리. 병자호란 항목은 광해군부터가 아니라
  "청이 군신 관계를 요구하다 거부당하자 침입한 전쟁"으로 시작하게 다시 썼습니다.
- 목적이 빠진 곳을 채움: 서원 철폐의 이유, 헤이그 특사의 목적, 직지심체요절이
  어떤 책인지.
- 한국사 항목 42 → 49개. 키워드는 옮기기만 해서 빠진 것이 없습니다.

**결과** (항목 여기저기 흩어진 핵심어 20개, 서빙 경로인 Ollama + 근거, greedy)

| 조건 | 맞음(✓=1, △=0.5) |
|---|---|
| 근거 없음 | 약 3.5 / 20 |
| 수정 전 항목 근거 | 11.5 / 20 |
| **수정 후 항목 근거** | **18 / 20** |

틀림 → 맞음: 병자호란("명과 후금의 전쟁"), 탕평책("사림의 균역을 통해"), 삼별초 일부,
광주학생항일운동(날짜 지어냄), 훈민정음("한국어의 표준어"), 물산장려운동(비판을 거꾸로
읽어 "자본가를 비판한 운동"). 남은 일부 틀림은 삼별초(신의군 구성을 삼별초 전체로 확대),
훈민정음(자음 17자를 모두 발음 기관 본뜸이라고 뭉뚱그림), 서원 철폐("향교·성균관 강화"를
지어냄), 균역법(다음 문장의 삼정 문란을 결과처럼 붙임)입니다.

**FAISS 재적재.** 한국사 FAISS 문서는 ConceptMap을 잘라 넣은 것이라, 항목을 고치면
옛 조각이 남습니다. 옛 조각은 새 ConceptMap과 일치하지 않아 근거에서는 빠지지만
"[관련 학습 자료]"에는 계속 보이고 고치기 전 오류도 담고 있습니다. 적재 스크립트는
추가만 할 줄 알아서 `--replace`를 더했습니다. 같은 출처·과목(`[대상]한국사 (폴더:
ConceptMap_한국사)`)의 기존 문서를 지우고 넣습니다.

```
python scripts/ingest_rag_docs.py --from-concept-map 한국사 --replace --dry-run
python scripts/ingest_rag_docs.py --from-concept-map 한국사 --replace
```

옛 조각 85개 삭제 → 698,347개(원래 코퍼스 수와 정확히 같음) → 새 조각 103개 추가 →
698,450개. 코퍼스 쪽 매핑 698,347개가 그대로인지, 새 조각 103개가 모두 ConceptMap
출처로 판별되는지(근거로 쓰일 수 있는지) 확인했습니다.

이 스크립트는 저장할 때 기존 `.bak`을 **지웠습니다**. 그 `.bak`은 5월에 만든 적재 전
원본 DB였습니다. 이제는 지우지 않고 `.bak.<수정일>`로 이름을 바꿉니다
(`index.faiss.bak.20260512150130`이 그 원본입니다). 백업이 쌓이면 수동으로 정리하세요
(한 벌에 약 2.7GB).

> **ConceptMap 한국사 항목을 고치면 반드시 `--replace`로 다시 적재해야 합니다.**
> 안 하면 고친 항목의 FAISS 조각이 근거에서 빠집니다(출처 판별이 문자열 일치라서).

서버에서 확인: `/report/한국사`의 병자호란은 이제 정묘호란(형제 관계)과 병자호란(군신
관계)을 구분해 맞게 설명합니다(보강 전에는 근거를 붙여도 "명과 후금의 전쟁"). 다만
ConceptMap 키워드가 아닌 모호한 질의("군신 관계")는 FAISS가 조각 여러 개를 섞어 주고,
모델이 그중 무관한 조각(훈구·사림)에 끌려 틀렸습니다. 키워드 밖 질의의 검색 품질은
남은 과제입니다.

### 검색 정밀도·FAISS 경로·재적재 점검 (2026-09-23)

한국사 보강 뒤 남은 오류를 따라가 보니 대부분 **검색이 불필요한 자료를 같이 붙여서**
생긴 것이었습니다.

**1. 포함되는 짧은 일치를 뺍니다(`search_concept_map`).** "서원 철폐"로 흥선대원군
항목이 걸렸는데, "서원"이라는 짧은 키워드로 과거제도 항목(성균관·향교)도 같이 걸려
모델이 "서원 철폐 후 향교·성균관을 강화했다"고 지어냈습니다. 맨 앞 항목의 일치 키워드
안에 통째로 들어가는 키워드로만 걸린 항목은 이제 뺍니다("조선 신분제"에서 `조선` 항목,
"가정법 미래"에서 `가정법` 항목 등). 617개 질의 중 22개만 바뀌었고 모두 이런 경우였습니다.

**2. 조사가 붙어도 찾습니다.** 앞쪽 단어 경계는 그대로 두고 뒤쪽에 조사(의·은·는·이·
가·을·를·와·과·에·로·도·만·에서·으로)를 허용했습니다. "홍경래의 난", "영조와 정조",
"유전과 진화"가 이제 키워드로 잡힙니다. 모든 키워드에 "…의 특징"을 붙여 재보니 자기
항목을 못 찾던 경우가 573건 → 0건, 순서가 어긋난 경우 0건입니다. 키워드 패턴 600여
개는 컴파일해 캐시합니다(re 모듈의 캐시 512개를 넘쳐 요청마다 다시 컴파일하고 있었습니다).

**3. FAISS 조각이 오면 첫 조각이 속한 항목 하나만 통째로 근거로 줍니다
(`grounding.usable_docs`).** 키워드 밖 질의는 서로 다른 항목의 조각 3개가 섞여 옵니다.
"군신 관계"에 병자호란 조각과 함께 과거제도 조각(훈구·사림)이 와서 모델이 "군신 관계란
훈구와 사림의 대립"이라고 답했습니다.

| 키워드 밖 한국사 질의 12개 (Ollama) | 맞음 |
|---|---|
| 조각 최대 3개 (이전) | 4 / 12 |
| **첫 조각의 항목 통째로** | **7 / 12** |

남은 실패였던 노비안검법·호패법·훈련도감도 **이름만 있고 정의가 없는 개념**이었습니다
(예: "광종(노비안검법·과거제…)"을 받은 모델이 "노비의 권리를 보호하는 법", 호패를
"호랑이와 사자"로 설명). 기존 항목 안에 짧은 정의를 넣고 키워드로 걸어 셋 다 맞게
됐습니다. 천리장성·서희·강감찬도 같은 방식으로 보강했습니다. `태종`은 키워드로 걸지
않았습니다 — "청 태종의 침입"이 조선 태종 항목에 걸리기 때문입니다.

**4. 항목 문장 고침.** 삼별초(신의군만 포로 출신임을 명시), 훈민정음(기본자만 본뜨고
나머지는 가획·합성), 균역법(전용 항목으로 떼고 삼정 문란은 "한계"로 서술), 서희("3차
침입:" 머리말을 읽고 "3차 때"라고 답해 "세 차례 침입했다"로 바꿈), 관계대명사 what(자료가
하나로 줄자 "what 앞에 선행사가 온다"로 부정을 뒤집어, 틀린 예 "the thing what I want ×"를
명시).

한국사 핵심어 20개는 이 변경 뒤 18.5/20입니다(18 → 18.5). 삼별초·훈민정음·서원 철폐·
균역법은 모두 맞게 됐지만, 같은 항목으로도 Ollama 출력이 실행마다 조금 달라서(temperature
0에서도) ±1개는 오차로 봐야 합니다.

**5. 기동 시 재적재 점검(`rag._check_concept_map_chunks`).** FAISS의 ConceptMap 조각이
현재 ConceptMap과 다르면 서버 로그에 경고합니다.

```
[FAISS 경고] 한국사 ConceptMap 조각이 현재 ConceptMap과 다릅니다 — 옛 조각 2개,
FAISS에 반영 안 된 항목 3개. 다시 적재하세요: python scripts/ingest_rag_docs.py
--from-concept-map 한국사 --replace
```

항목을 고친 직후 실제로 이 경고가 떴고, 재적재 후 사라지는 것을 확인했습니다.

**서빙 출력이 중간에 잘립니다.** `_concept_explain()`의 `max_new_tokens=200`에서
설명이 목록 중간("2.")에 끊기는 경우가 여럿 보였습니다(가정법 미래 첫 생성이
were to 설명 전에 끊김). 늘리면 문항당 생성 시간이 늘어나므로 따로 정해야 합니다.

### ConceptMap 확충 (13 → 42개 항목)

한국사는 ConceptMap이 **유일한 지식 출처**인데 항목이 13개뿐이라, 시대를
통으로 묶은 요약("고려", "조선")만 있고 개별 사건·제도는 담기지 못했습니다.

더 큰 문제는 키워드 설계였습니다. `_concept_map_search`는 **키에 있는 말만**
찾습니다. "갑오개혁"과 "신간회"는 설명 본문에는 있었지만 키워드가 아니어서
매칭이 실패했고, 그래서 위에 적힌 오검색이 났습니다.

그래서 두 가지를 같이 고쳤습니다.

- 항목을 13개 → **42개**로 확충 (전체 ConceptMap 120 → 149개). 고려 대외항쟁,
  조선 통치체제·수취제도·신분제, 임진왜란·병자호란, 붕당정치, 흥선대원군,
  갑신정변, 동학농민운동, 갑오개혁, 국권피탈, 항일의병, 3·1운동과 임시정부,
  무장독립투쟁, 실력양성운동, 6·25, 4·19, 유신, 5·18, 6월 민주항쟁,
  경제성장, 통일정책, 독도·간도 등.
- 키워드에 **고유명사를 직접** 넣었습니다(을사늑약·신간회·대동법·윤봉길·
  서희·전봉준·박종철 등). 설명 본문에만 있는 말은 검색되지 않기 때문입니다.

검증: 기존에 실패하던 "갑오개혁"·"신간회"를 포함해 15개 대표 질의가 모두
정확한 항목에 매칭됩니다. 중복 키·과목 내 중복 키워드는 없습니다
(딕셔너리 리터럴은 중복 키를 조용히 덮어쓰므로 AST로 별도 확인).

---

## RAG 개선 (2026-09-20)

한국사 ConceptMap을 FAISS DB에 적재하면서 검색 경로 전반을 손봤습니다.
`scripts/ingest_rag_docs.py`로 잰 값입니다 — ConceptMap 키워드 149개를 질의로
넣어 상위 3건 안에 질의어가 들어오는 비율입니다.

| 조건 | 수학 | 영어 | 국어 | 과학 | 사회 | 한국사 | **전체** |
|---|---|---|---|---|---|---|---|
| 기존 (보강 질의 + 전역 상위 30 + 필터) | 43% | 15% | 56% | **64%** | 64% | 33% | 43% |
| 원본 질의 + 전역 + 필터 | 49% | 20% | 50% | 55% | **71%** | 69% | 54% |
| **원본 질의 + 과목 한정** | **57%** | **35%** | **56%** | 55% | 64% | **81%** | **61%** |

### 고친 것 세 가지

**1. 질의 보강 제거.** `f"{subject} 교육과정 {query} 개념 학습"`으로 부풀리고
있었는데, 임베딩 모델이 128토큰이라 짧은 질의에서는 붙인 일반어가 벡터를
지배해 '교육과정'을 논하는 엉뚱한 문서로 끌려갔습니다. 빼는 것만으로 +11%p
(한국사 포함 149건 기준. 한국사를 빼면 확정되지 않음 — 아래 재검증 참고).

**2. 과목 한정 검색 (FAISS IDSelector).** 전역 상위 30건을 뽑아 과목 필터를
걸던 방식은, 문서가 적은 과목이 애초에 상위 30에 못 들어 검색이 안 됐습니다.
해당 과목 문서만 놓고 찾도록 바꿨습니다. +7%p(같은 단서)이고 **더 빠릅니다**
(한국사 2.4ms / 과학 14.5ms / 수학 37.7ms vs 전역 74.8ms). 속도 이득은
정확도와 무관하게 실측된 값입니다.

**3. 과학·사회 경로 패턴 수정.** `_SUBJECT_PATH_PATTERNS`에서 두 과목의
번호가 서로 바뀌어 있었습니다(과학은 실제 `_05`, 사회는 `_04`). 두 번째
패턴이 받아내서 동작은 했지만 첫 패턴은 0건이었습니다.

##### 짝지은 재검증 — 개선 폭을 과장했습니다

위 표는 과목별 적중률을 나란히 놓은 것입니다. 같은 질의로 두 방식을 쟀으므로
질의별로 짝지어 McNemar 검정을 다시 했습니다(한국사 제외 — 옛 방식은 한국사를
FAISS에서 아예 제외했기 때문에 비교 대상이 아닙니다).

| 과목 | n | 옛 방식만 맞음 | 새 방식만 맞음 | McNemar p |
|---|---|---|---|---|
| 수학 | 35 | 2 | 7 | 0.180 |
| 영어 | 20 | 0 | 4 | 0.125 |
| 국어 | 16 | 2 | 2 | 1.000 |
| **과학** | 22 | **5** | **3** | 0.727 |
| 사회 | 14 | 2 | 2 | 1.000 |
| **합계** | 107 | 11 | 18 | **0.265** |

- **과학의 64% → 55% 하락은 잡음입니다**(5건 대 3건, p=0.727). 우려할 필요가
  없습니다.
- 그러나 **개선도 확정되지 않았습니다.** 방향은 수학·영어에서 새 방식 쪽이지만
  과목별로도 합계로도 유의하지 않습니다(p=0.265). 위 표의 "43% → 61%,
  +18%p"는 **한국사 42건이 크게 기여한 값**입니다. 한국사에서는 옛 방식이
  애초에 작동하지 않았습니다(33% → 81%).
- 정직하게 말하면, 한국사 편입의 효과는 분명하고, 나머지 5개 과목의
  질의 보강 제거·과목 한정 효과는 **방향은 좋지만 통계적으로 확정하지 못했습니다.**
  과목 한정 검색이 더 빠르다는 점(한국사 2.4ms / 수학 37.7ms vs 전역
  74.8ms)은 정확도와 무관하게 실측된 이득입니다.

### 한국사를 FAISS에 편입

`_FAISS_EXCLUDED_SUBJECTS`에서 한국사를 뺐습니다. 근거는 같은 42개 질의로 잰
비교입니다.

| 한국사 검색 경로 | 상위 3건 적중 |
|---|---|
| FAISS 과목 한정 + 청크 분할 (98건) | **81%** |
| 기존 `_concept_map_semantic` (42건) | 33% |

기존 경로가 33%인 이유가 중요합니다. `_concept_map_semantic`은 ConceptMap
항목을 그대로 임베딩하는데 **모델이 128토큰에서 자릅니다.** 한국사 항목은
중앙값 162토큰, 최대 277토큰이라 42건 중 38건이 뒷부분을 잃습니다.
적재 시 문장 경계로 나눠 넣으면(42건 → 85조각) 그 부분도 검색됩니다.
기존 잡문서 13건이 결과에 섞이는 비율은 9%에 그쳤습니다.

### 적재 도구 (`scripts/ingest_rag_docs.py`)

기존 `scripts/add_concept_docs.py`는 문서를 소스코드에 하드코딩해야 해서
재사용이 안 됐습니다. 새 스크립트는 ConceptMap이나 JSONL에서 읽습니다.

```
python scripts/ingest_rag_docs.py --from-concept-map 한국사 --dry-run
python scripts/ingest_rag_docs.py --from-jsonl data/new_docs.jsonl
```

설계에서 신경 쓴 점:

- **저장 텍스트와 임베딩 텍스트를 분리.** 문서 머리말(`[대상]`·핵심어 나열)이
  128토큰 예산의 45%(중앙값 58토큰)를 먹어 정작 본문이 잘립니다. 저장은
  기존 형식대로, 임베딩은 "과목 + 본문"만 합니다.
- **문장 경계 청크 분할.** 본문이 예산을 넘으면 마침표 뒤에서 나눕니다.
- **`langchain_core.documents.Document` 사용.** 기존 스크립트는 `_Doc`
  클래스를 스크립트 안에 정의해서 pickle에 `__main__._Doc`으로 저장되는데,
  `rag.py`가 역직렬화할 때 그 클래스를 못 찾아 DB 로드가 통째로 실패합니다.
  현재 DB 698,347건이 전부 langchain Document인 걸 보면 그 스크립트는 실행된
  적이 없는 것으로 보입니다.
- **원본을 덮어쓰지 않음.** 임시 파일에 쓰고 원본을 `.bak`으로 옮긴 뒤 교체합니다.
  2.1GB 인덱스를 직접 덮어쓰다 중단되면 DB가 깨집니다.
  (실제로 첫 시도가 CUDA 오류로 죽었는데 DB는 무사했습니다.)
- **중복 제거**와 `--dry-run`.
- `--device` 기본값은 `cpu`입니다. 적재는 배치 작업이라 CPU로 충분하고
  서빙 GPU와 다투지 않습니다.

적재 결과: 698,347 → **698,432** 벡터, 한국사 문서 13 → **98**건.
(2026-09-23 한국사 항목 보강 후 `--replace`로 다시 적재: **698,453** 벡터, 한국사 문서
13 + 106 = **119**건. "한국사 ConceptMap 보강"·"검색 정밀도" 절 참고.)

---

## RAG 검색 순서

`rag.py`의 `subject_aware_search`:

1. 개요 쿼리("핵심 개념 및 자주 출제되는 단원") → ConceptMap 과목 샘플
2. ConceptMap 키워드 매칭 → 히트하면 즉시 반환 (FAISS 섞지 않음)
3. 한국사 → ConceptMap 의미 유사도 상위 k개
4. FAISS 벡터 검색 + 과목 필터(경로 패턴 → 키워드 순)
5. 전부 실패 → ConceptMap 과목 샘플

ConceptMap을 FAISS보다 우선하는 것은 의도된 설계입니다. 손으로 정리한
압축 노트가 원본 코퍼스 조각보다 정보 밀도가 높기 때문입니다. ConceptMap에
없는 개념은 4번에서 FAISS가 정상적으로 처리합니다.

임베딩은 `jhgan/ko-sroberta-multitask`, 인덱스는 FAISS(정규화 + 내적)입니다.

---

## Ollama 의존성

Ollama(`localhost:11434`의 `llama3.1:latest`)는 폴백이 아니라 **주 경로**이기도
합니다.

- `/api/ai/report/math`, `/report/writing`, `/report/premium`의 LLM 문단은
  **Ollama만** 씁니다.
- 개념 설명(`_concept_explain`)은 한국사와 생성 실패 시 Ollama로 넘어갑니다.
- Backend의 `MathAiService`도 Ollama를 직접 호출합니다.

**Ollama가 없으면 에러 없이 조용히 해당 섹션이 사라집니다** (`_ollama_analyze`가
`None`을 반환하고 호출부가 생략). 리포트에 "AI 개념 분석"이 안 보이면 Ollama부터
확인하세요.

---

## 알려진 과제

### 마무리됨 — 수학 어댑터 정확도

`정답:` 이후 값·순서·데이터 세 축을 전부 재본 결과, 짧거나 중간 길이의 풀이
텍스트로 LoRA 미세조정하는 파이프라인은 어떻게 바꿔도 25~29%대에 갇힌다.

- 평가 기준부터 잘못돼 있었다. 객관식 정답률로 부르던 값은 **보이지 않는
  보기 번호를 추측한 것**이었다(71859 문항 본문에 보기가 없다). 값 일치로
  다시 재는 `math_value_match.py`를 만들었다(우연 일치율 0.42%로 검증).
- `풀이 → 정답` 순서 재배열(CoT): 차이 없음 (n=300, McNemar p=0.44).
- 71718로 재학습(풀이 3배 풍부, 계산 연쇄 73.8%): 기존·CoT·베이스끔
  어느 것과도 차이 없음(p=0.38~0.70).
- **베이스 Qwen3-8B의 자체 추론 모드는 모든 미세조정 변형을 유의하게
  앞선다**(35.8% 대 25.5~28.9%, p≤0.0015). 미세조정이 베이스에 무언가를
  더한 적이 한 번도 없다. 다만 6배 느리다(문항당 34초).
- **서빙에 수학 풀이를 소비하는 기능이 없다.** 코드베이스를 조사했으나
  계획의 흔적도 없다(`ocr/grading.py`의 채점은 정답을 외부에서 미리 받는
  방식이라 LLM 풀이가 불필요, 백엔드 연동 API에도 관련 스키마 없음).

**결론: 데이터를 더 구하는 방향의 개선은 막다른 길이고, 소비처가 없어
투자할 이유도 없다.** 이 방향의 추가 작업은 중단한다. 상세 근거는
"수학 어댑터" 절과 "71718 학습 결과", "서빙에는 수학 풀이의 소비처가
없습니다" 절에 있다.

### 수학 학습 데이터 후보 (D 드라이브 실측, 2026-09-21)

`D:/`에 AI-Hub 수학 데이터 세 개가 이미 내려받아져 있습니다. 처음에는 웹 소개
페이지만 읽고 후보를 정리했는데, 실제 파일을 열어 보니 달라진 부분이 있어
확인된 사실로 다시 썼습니다.

| 폴더 | AI-Hub | 상태 | 확인한 규모 |
|---|---|---|---|
| `수학교과문제풀이데이터` | 71859 | 압축 해제됨, **현재 학습에 쓴 데이터** | JSON 18,280건 (TL 16,248 / VL 2,032), 그중 156건은 읽을 수 없음 |
| `수학 과목 문제생성 데이터` | 71718 | 압축 해제됨 | 학습 74,882 + 검증 8,321 = **83,203건** (웹 공표는 84,203) |
| `수학 과목 자동 풀이 데이터` | 71716 | **zip 24개, 압축 안 풀림** | 문제·모범답안 각 30,053건(학습) + 손글씨 풀이 라벨 |

`train/preprocess.py`가 읽던 `C:/수학교과문제풀이데이터`는 지금 `D:`로 옮겨져
있습니다. 원본 JSON 18,280건에서 전처리가 문항·정답 텍스트가 모두 있는 것만
남겨 14,395건(학습 12,640 / 평가 1,755)이 됐습니다. 이 폴더의 라벨 클래스가
71859 소개 페이지와 일치하는 것으로 71859임을 확인했습니다.

`D:/교과 단계별 데이터`의 수학 폴더는 교과서 본문과 빈칸형 QA라 문제-풀이
데이터가 아닙니다. RAG 코퍼스의 수학 18.6만 건이 이것입니다.

#### 71718 (문제 생성) 구조

문제와 모범답안이 **파일명으로 짝지어집니다**(`..._35793.json` ↔ `..._35793_A.json`).
JSON은 UTF-8 BOM으로 시작해 `utf-8-sig`로 읽어야 합니다.

- 문제: `OCR_info[0].question_text` (LaTeX 본문, 선택형의 보기는 본문 안에 포함)
- 모범답안: `answer_info[0].answer_text` = **풀이 본문**,
  `answer_bbox`의 `type: answer` 항목 `text` = **최종 정답**(보기 번호 또는 값)
- 문항 유형: `question_type1` = 선택형 / 단답형

> 웹 소개 페이지 요약은 `answer_text`에 풀이가 들어 있다고 했고 맞았습니다.
> 다만 `answer` 항목의 `text`가 정답만이 아니라 보기 번호 뒤에 손 계산이 이어지는
> 경우(`⑤ \[ \begin{array}...`)도 있어, 정답 추출은 전처리에서 확인해야 합니다.

**풀이 품질은 학년별로 크게 다릅니다.** 학년별 400건 표본(중2만 1,500건)입니다.

| 학년 | 학습 건수 | 선택형 | 풀이 중앙값 | 등호 3개 이상 | 60자 미만 |
|---|---|---|---|---|---|
| 초3 | 11,544 | 10% | 84자 | 32% | 34% |
| 초4 | 8,218 | 14% | 77자 | 19% | 40% |
| 초5 | 9,719 | 13% | 115자 | 40% | 26% |
| 초6 | 9,251 | 12% | 112자 | 47% | 26% |
| 중1 | 10,674 | 42% | 150자 | 58% | 12% |
| 중2 | 10,236 | 45% | 238자 | 76% | 7% |
| 중3 | 9,429 | 41% | 253자 | 87% | 6% |
| 고1(공통수학) | 5,811 | 20% | 168자 | 78% | 6% |

현재 71859 학습 데이터(풀이 9,751건)는 중앙값 94자, 등호 3개 이상 39.6%,
60자 미만 34.7%입니다. 중학교 이상은 71718이 계산 과정이 훨씬 풍부하고,
초등 3~4학년은 현재 데이터와 비슷한 짧은 설명입니다.

**분포가 평가와 맞지 않습니다.** 71718 전체의 선택형은 **25.5%**
(19,095 / 74,882)인데, 평가셋은 객관식이 63.9%입니다. "선택형을 포함해서
평가 형식과 맞는다"고 처음 쓴 것은 과장이었습니다. 71718로 학습하면 풀이
과정은 풍부해지지만 보기 번호를 고르는 형식은 덜 연습하게 됩니다.

#### 오염 검사 (`train/overlap_check.py`)

평가셋은 71859의 `VL_`에서 왔으므로, 새 데이터가 같은 문제를 담고 있으면
점수가 부풀려집니다. 문제 본문을 정규화해 직접 비교했습니다(id는 데이터셋마다
체계가 달라 쓸 수 없습니다).

| 비교 | 결과 |
|---|---|
| 71718 학습 74,845건 중 71859 평가 문항과 정확히 동일 | **8건 (0.01%)** |
| 71859 평가 1,669건 중 71718 학습 문항과 유사도 ≥ 0.95 | 12건 (0.7%) |
| 〃 유사도 ≥ 0.9 | 29건 (1.7%) |
| 〃 유사도 ≥ 0.8 | 100건 (6.0%) |
| 71716과 71718의 id 교집합 | 0건 (본문 대조는 못 함) |

(유사도: 문자 3-gram TF-IDF 코사인, 평가 문항마다 최근접 학습 문항)

오염은 작습니다. 학습 전에 유사도 0.9 이상인 71718 문항만 걷어내면 됩니다.

**기존 데이터에서 발견한 것:** 71859 **안에서** 학습과 평가가 정확히 겹치는
문항이 **47건**입니다(평가 고유 1,669건의 2.8%). 지금까지 잰 수학 정확도는
이만큼 부풀어 있을 수 있습니다. 크지 않지만 알고 있어야 합니다.

#### 71716 (자동 풀이)

압축이 풀려 있지 않아 내부 구조를 끝까지 보지 못했습니다. 웹 소개는 서술형
100%라고 하고, 파일명 체계는 71718과 같습니다(`M2_1_01_...`). 문제·모범답안이
각 30,053건이라 71718(74,882)보다 작고 서술형뿐이라, 객관식 평가에는 더 안
맞습니다. **손글씨 풀이 183,085건은 이미지 기반이라 텍스트 전용 모델에는 쓸 수
없습니다.** 71718보다 우선순위가 낮습니다.

#### 그래서 새 데이터가 정확도를 올릴까

**모릅니다.** 근거를 나눠 적습니다.

- 풀이의 계산 과정이 풍부한 것은 **확인됐습니다**(중학교 이상).
- 그러나 현재 데이터의 풀이도 39.6%는 이미 계산 연쇄이고, CoT 재학습에서
  그 40%가 들어 있었는데 차이가 없었습니다(n=300, McNemar p=0.44/0.23/1.00).
- 71718은 선택형이 25.5%로 평가 분포와 다릅니다.

더 먼저 볼 것이 있습니다. 베이스 Qwen3-8B에는 자체 추론 모드
(`enable_thinking=True`)가 있는데, 지금은 짧은 풀이로 미세조정하고 추론 모드를
꺼서만 씁니다. **베이스의 추론 모드가 같은 객관식 204문항에서 몇 %인지 재본 적이
없습니다.** 36.8%보다 크게 높다면 새 데이터를 붙이기 전에 접근 자체를 다시 봐야
합니다.

#### 학습에 쓰게 되면

- **중학교 이상만 쓰는 방안**: 계산 과정이 풍부한 구간이라 초등 3~4학년의
  짧은 설명을 걸러내면 풀이 품질이 균일해집니다. 다만 초등 문항은 평가셋에도
  들어 있어 학습에서 빼면 초등 성능이 떨어질 수 있습니다.
- **선택형 보강**: 25.5%라 분포가 어긋나므로 71859의 객관식과 섞어 비율을
  평가에 맞추는 것이 좋습니다.
- **정답 추출 검증**: `answer` 항목의 `text`에 손 계산이 섞인 경우가 있어,
  전처리 후 100건을 뽑아 정답이 보기 번호로 정리됐는지 확인합니다.
- **이용 조건**: 세 데이터 모두 "내국인만 신청 가능, 승인 필요"입니다. 이미
  내려받으셨으니 약관상 학습·서비스 사용 범위를 확인해 주세요.

### 해결됨

- **개념 설명 품질 평가 부재** → `test_concept_explain_eval.py`.
  ConceptMap을 참조 답안으로 쓰고 변별력(정답 참조 vs 다른 개념 참조)을
  주지표로 삼아, 정답이 하나로 정해지지 않는 문제를 우회했다.
- **한국사 ConceptMap 빈약** → 13개 → 42개 항목으로 확충하고 고유명사를
  키워드에 직접 넣어 "갑오개혁"·"신간회" 오검색을 고쳤다.
  (어댑터 학습 데이터 확보는 여전히 미해결)
- **글쓰기 1점 클래스** → 문제가 아니었다. 777건으로 재보니 1점 재현율
  0.733이다. 기존 결론은 1점 4건을 보고 내린 것이었다.

### 새로 드러나서 조치한 것

- **개념 설명에서 어댑터가 베이스보다 못하다** — 길이 중립 지표(개념F1)로
  5개 과목 전부 베이스가 앞선다(0.119 vs 0.189). **수학은 개념 설명의
  100%를 "정답:/풀이:" 문제풀이 형식으로 답했고**, **영어는 학습 답안이
  중앙값 20자라 한 문장만 내놓는다.**
  → **개념 설명은 전 과목 베이스로 전환.** 짝지은 부트스트랩에서 전체
  +0.070 [+0.043, +0.097]로 유의하다. 국어만 개별 구간이 0을 포함하지만
  n=16의 검정력 문제이지 국어가 다르다는 근거가 아니다.
  베이스 출력의 마크다운·이모지는 `_strip_markdown()`으로 정리한다.
  어댑터가 나은 축도 있다: 밀도(꺼낸 개념어 중 맞은 비율)는 4과목에서
  어댑터가 앞선다. 짧지만 정확하되, 3배의 길이 차이를 덮지 못한다.
- **교과 어댑터 4개가 서빙에서 쓰이지 않게 됐다** — `_concept_explain()`
  에서만 쓰이던 것이다. 학습 형식 프롬프트로도 회복되지 않아
  (격차 +0.070 → +0.105) **로드를 끊었다**(`LOAD_SUBJECT_ADAPTERS=False`,
  약 668MB 절약).
- **글쓰기 채점 능력이 서빙에서 쓰이지 않았다** — `/api/writing/evaluate`의
  점수는 키워드 일치율 70% + 길이 비율 30%의 규칙 기반이고, QWK 0.54의
  파인튜닝 채점기는 피드백 문장 생성에만 쓰였다.
  → `final_score`는 그대로 두고 `llm_score`/`llm_score_raw` 선택 필드로
  채점기 의견을 함께 실어 보낸다. 실제 트래픽에서 두 점수를 비교해본 뒤
  교체 여부를 정하면 된다. 하위 호환은 깨지지 않는다.

### 미해결

- **`/api/writing/*`에 소비처가 없다** — Backend·Front 어디서도 호출하지 않고,
  서술형 답안을 제출받는 기능 자체가 없다. 글쓰기 어댑터 로드는 끊었다
  (`LOAD_WRITING_ADAPTER=False`). 연결하려면 제출 화면·문항(모범답안·키워드)
  관리·API를 새로 만들어야 하는 제품 결정이다.
- **개념 설명이 여전히 틀리는 경우가 있다** — ConceptMap 근거로 한국사 3.5 → 6.5/8,
  영어 3 → 10.5/12, 국어 5.5 → 11.5/12로 올랐다(위 절들). 수학·과학·사회는 근거 없이도
  거의 맞았지만 표본이 13개뿐이다. 근거를 붙이면 항목의 오류도 그대로 전달되므로
  ConceptMap 내용 자체의 정확성이 곧 서빙 품질이다. 사실 정확도는 개념F1이 아니라
  출력을 읽어 판정해야 한다(`test_grounded_explain_eval.py`).
- **개념 설명이 200토큰에서 잘린다** — 목록 중간에 끊기는 출력이 있다. 늘릴지는
  생성 시간과 저울질해서 정할 것.
- `rpi/pose_hailo.py`의 `decode_keypoints()` 미구현 — **Hailo 실물 없이는
  진행 불가**. 추측으로 구현하면 예외 대신 '그럴듯하지만 틀린 좌표'가 나오고,
  그 값이 `classify_posture()`를 거쳐 백엔드 학습활동 기록까지 올라간다.
  Pi5에서 `describe_outputs()`로 텐서 형태를 확인하는 것이 선행 조건이다.
  지금은 `NotImplementedError`를 `rpi/pi_main.py`가 잡아 포즈만 건너뛰므로
  틀린 값이 올라가지는 않는다.
- **개념 설명 커버리지 지표가 길이에 휘둘린다** — 짧지만 옳은 답이 낮게
  나온다. 평균 출력 길이를 같이 찍게 해뒀지만, 길이로 정규화한 지표나
  요약 후 비교 같은 보완이 필요하다.
- **AI-Hub 수학 데이터(71718·71716) 이용 조건 미확인** — 로컬에 약관 문서가
  없다. 서빙하는 수학 어댑터가 없어 지금은 영향이 없다.

### 서빙과 무관해져 보류한 것

- **국어 개념 설명 판정 미확정** — 신뢰구간 [−0.017, +0.078]. 이미 베이스로
  전환했고 교과 어댑터는 로드하지 않으므로, 어댑터를 되살릴 때만 의미가 있다.
- **`_concept_explain()` 프롬프트와 교과 어댑터 학습 형식의 불일치** — 개념
  설명에서 어댑터를 쓰는 과목이 더는 없다. 학습 형식으로 맞추는 실험도
  이미 했다(격차가 오히려 벌어짐).
- 글쓰기 데이터에 **5점이 0건** — 학습 14,223건·평가 777건 모두. system
  프롬프트는 "1점부터 5점"이라고 하지만 실제 라벨 공간은 1~4다.
  `llm_score`도 1~4만 낸다. `/api/writing/*`에 소비처가 없어 제품 영향은 없다.

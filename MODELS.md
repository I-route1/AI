# 모델 구성

i-Route AI 서버가 서빙하는 모델과 학습 내역을 정리한 문서입니다.

> 어댑터 실물과 학습 데이터는 용량 때문에 `.gitignore` 대상(`train/`)입니다.
> 저장소에는 학습·평가 스크립트만 있고, 이 문서가 그 결과를 기록하는 역할을 합니다.

---

## 서빙 구조

베이스 모델 **하나**에 LoRA 어댑터 6개를 모두 올려두고, 요청마다
`set_adapter()`로 갈아끼웁니다.

```
unsloth/Qwen3-8B-unsloth-bnb-4bit  (4bit NF4, bf16 compute, double quant)
├── math     train/math_adapter_qwen
├── writing  train/writing_adapter_qwen_weighted
├── korean   train/korean_adapter_qwen
├── english  train/english_adapter_qwen
├── science  train/science_adapter_qwen
└── social   train/social_adapter_qwen
```

설정은 `src/api/adapters.py`에 모여 있고 `src/api/main.py`가 기동 시 로드합니다.

**왜 베이스를 하나로 합쳤나** — 예전에는 수학이 Gemma4, 글쓰기가 원격 Llama3.1
어댑터라 베이스가 서로 달랐습니다. 8B 모델 두 개를 동시에 띄워야 해서 VRAM이
부족했고 OOM이 났습니다. 두 어댑터를 같은 Qwen3-8B 베이스로 재학습해 합쳤습니다.

**리소스** (RTX 5070 Ti, 16GB 기준 실측)

| 항목 | 값 |
|---|---|
| 4bit 베이스 | 약 6 GB |
| 어댑터 6개 | 약 667 MB (개당 166.6 MB) |
| 합계 | 약 7 GB |
| 어댑터 6개 로드 시간 | 4.7초 |
| 개념 설명 1회 생성 | 5~15초 (200 토큰 기준) |

GPU 생성은 블로킹이라 FastAPI에서 `run_in_threadpool`로 분리해 호출합니다.

---

## 어댑터별 학습 내역

공통 LoRA 설정: **r=16, alpha=32, dropout=0.05**,
target_modules = `q_proj, k_proj, v_proj, o_proj, gate_proj, up_proj, down_proj`
(7개). 옵티마이저 AdamW, LR 2e-4, warmup 5%, batch 1 × grad_accum 16.
학습 루프는 HF Trainer가 아니라 순수 PyTorch 커스텀 루프이며,
eval_loss가 갱신될 때만 체크포인트를 저장합니다.

| 어댑터 | 학습 데이터 | 학습/평가 건수 | epochs | max_len | best eval_loss |
|---|---|---|---|---|---|
| math | AI-Hub 수학교과문제풀이 | 12,640 / 1,755 | 2 | 640 | **0.4381** |
| writing | 글쓰기 평가·피드백 | 28,500 / 1,500 | 3 | 896 | **0.3991** |
| korean | AI-Hub 교육과정(국어) | 15,200 / 800 | 2 | 768 | **0.3215** |
| english | AI-Hub 교육과정(영어) | 15,200 / 800 | 2 | 768 | **0.3247** |
| science | AI-Hub 교육과정(과학) | 15,200 / 800 | 2 | 768 | **0.3529** |
| social | AI-Hub 교육과정(사회) | 15,200 / 800 | 2 | 768 | **0.3066** |

### 프롬프트 형식

전부 Qwen3 채팅 템플릿(system / user / assistant)을 씁니다. 추론 시
`apply_chat_template(..., enable_thinking=False)`를 **반드시** 넘겨야 합니다.
학습 데이터에 `<think>` 블록이 없어서, 켜두면 모델이 빈 사고 블록을 먼저
내보내며 파인튜닝된 동작이 어긋납니다.

system 프롬프트는 학습 때 쓴 문구와 **글자 단위로 같아야** 합니다.
`src/api/adapters.py`의 값과 `train/preprocess_curriculum.py`의 `SUBJECTS`가
일치해야 하며, 한쪽만 고치면 성능이 떨어집니다.

교과 어댑터 입력 형식:

```
[학습 지문]
{지문}
[교육과정 성취기준]
{[9국03-01] 형태의 성취기준}
질문: {질문}
```

---

## 평가

### 교과 어댑터 (국어·영어·과학·사회)

`test_subject_adapters_eval.py`로 측정합니다. 같은 샘플을 **어댑터 적용**과
**어댑터 비활성(=베이스 모델)** 두 조건으로 생성해 비교합니다. 그 차이가
파인튜닝의 순수 기여분입니다.

한국어는 교착어라 어절 단위 토큰화가 불안정해 **문자 단위**로 잽니다.
평가는 greedy 디코딩(`do_sample=False`)이라 재현 가능합니다.

- `char-F1` — 정답/생성의 문자 다중집합 겹침 F1 (SQuAD F1의 문자판)
- `ROUGE-L` — 최장공통부분수열 기반 F1
- `EM` — 공백 제거 후 완전일치

```
python test_subject_adapters_eval.py --n 40
```

**결과** (과목당 40건, `--max-new-tokens 128`)

| 과목 | char-F1 (어댑터 / 베이스 / Δ) | ROUGE-L (어댑터 / 베이스 / Δ) | EM (어댑터 / 베이스) |
|---|---|---|---|
| 국어 | 0.836 / 0.283 / **+0.553** | 0.816 / 0.257 / **+0.559** | 0.375 / 0.000 |
| 영어 | 0.737 / 0.305 / **+0.432** | 0.724 / 0.283 / **+0.441** | 0.225 / 0.025 |
| 과학 | 0.692 / 0.357 / **+0.335** | 0.633 / 0.277 / **+0.356** | 0.125 / 0.000 |
| 사회 | 0.758 / 0.306 / **+0.453** | 0.714 / 0.244 / **+0.471** | 0.200 / 0.000 |

4과목 모두 어댑터가 베이스를 앞섭니다 (평균 char-F1 Δ **+0.443**).
베이스 모델의 EM이 사실상 0인 것이 특징적입니다. Qwen3-8B는 일반 지식으로
그럴듯한 문장을 만들지만, 교과서 답안의 표현 방식과 길이를 맞추지는
못합니다. 파인튜닝이 지식보다 **출력 형식과 답변 톤**을 학습시킨 셈입니다.

과학의 향상 폭이 가장 작습니다(+0.335). 베이스 char-F1도 과학만 0.357로
높은데, 과학 답안이 일반 상식과 겹치는 부분이 많아 베이스가 원래 어느 정도
맞히기 때문으로 보입니다.

### 글쓰기 어댑터

서술형 답안에 1~5점을 매기는 채점 정확도로 평가했습니다.

| 버전 | 정확도(±0) | ±1 이내 | 평균 오차 | best eval_loss |
|---|---|---|---|---|
| **weighted (서빙 중)** | **51%** | 99% | 0.50 | 0.3991 |
| weighted_v2 | 44% | 96% | 0.60 | 0.4012 |

`weighted`는 채점 클래스 불균형을 손실 가중치로 교정한 버전입니다
(sklearn `balanced` 방식, 상한 5.0). 채점 샘플에만 가중치가 걸리고 서술형
피드백 샘플은 1.0이며, eval 손실은 가중치 없이 계산해 다른 실행과 비교
가능하게 했습니다.

`v2`는 여기에 1점 샘플 오버샘플링을 더한 버전입니다. 1점이 전체의 2.7%(386개)
뿐이라 가중치만으로는 한 번도 못 맞히는 문제를 풀려는 시도였는데, **정확도가
51%에서 44%로 떨어져 실패했습니다.** 서빙에는 `weighted`를 씁니다.

### 수학 어댑터

`test_math_adapter_eval.py`로 측정합니다. 수학 정답은 `정답: ③ 50` 처럼 짧고
형식이 정해져 있어서 char-F1을 주지표로 쓰면 풀이가 길수록 점수가 오르는
엉뚱한 신호가 나옵니다. `정답:` 줄만 뽑아 LaTeX·공백을 정규화해 비교합니다.

```
python test_math_adapter_eval.py --n 100
python test_math_adapter_eval.py --n 100 --text-only   # 그림·표 참조 문항 제외
```

**결과** (각 100건)

| 지표 | 어댑터 | 베이스 | 어댑터(텍스트 전용) |
|---|---|---|---|
| 정답 일치 | 10.0% | 0.0% | 11.0% |
| 객관식 일치 | **35.2%** (n=71) | 0.0% | **36.4%** (n=66) |
| char-F1 (정답줄) | 0.555 | 0.020 | 0.609 |
| `정답:` 형식 준수 | **100%** | 4.0% | 100% |

**형식은 완벽히 학습했지만 정답률은 낮습니다.** 5지선다 무작위 기대값이 20%인데
객관식 정답률이 36% 수준입니다. 우연보다는 낫지만 교과 어댑터(char-F1 0.69~0.84)에
비하면 확연히 떨어집니다. 전형적인 오답 양상은 형식은 맞고 값만 틀리는 것입니다:

```
정답   : ③ $40^{\circ}$
어댑터 : ③ $70^{\circ}$
```

두 가지를 짚어둘 필요가 있습니다.

**베이스의 0%는 실력이 아니라 형식 문제입니다.** 베이스는 `정답:` 형식을 4~8%만
지켜서("주어진 문제는 **이등변삼각형**의 성질과…" 식으로 서술) 답 추출 자체가
되지 않습니다. 따라서 베이스와의 정답률 비교는 의미가 약하고, 실질적인 기준은
**객관식 36% vs 무작위 20%** 쪽입니다.

**그림 문항 때문이 아닙니다.** eval의 28%가 `다음 그림에서…`처럼 텍스트 전용
모델이 구조적으로 못 푸는 문항이라 이를 제외하고 재측정했지만, 정답률은
10.0%→11.0%, 객관식은 35.2%→36.4%로 거의 변하지 않았습니다. 평가셋이 불리해서가
아니라 실제 수리 추론 능력이 부족한 것입니다.

> 다만 이 평가는 **문제 풀이** 능력을 잰 것이고, 현재 서빙에서 수학 어댑터가
> 실제로 하는 일은 `_concept_explain()`의 **개념 설명**입니다. 둘은 다른
> 과제이므로 이 수치를 곧바로 "서빙 품질이 나쁘다"로 읽으면 안 됩니다.
> 개념 설명 품질에 대한 정량 평가는 아직 없습니다.

---

## 한국사만 다른 이유

한국사는 **어댑터도 RAG 코퍼스도 없습니다.**

- AI-Hub 교육과정 데이터에 한국사 과목이 없어 어댑터를 학습하지 못했습니다
  (`train/preprocess_curriculum.py`는 국어/영어/과학/사회 4과목만 대상).
- FAISS 코퍼스 698,347건 중 한국사 문서는 **13건**뿐입니다. 참고로 다른
  과목은 7.6만~18.7만 건입니다.

문서가 이렇게 적으면 과목 필터가 사실상 무력화돼, 키워드 필터("조선", "고려",
"신라"…)에 아무 문서나 걸립니다. 실제로 "신간회" 검색에 발해 역사책 설명이,
"갑오개혁"에 동학농민운동 폐정개혁안이 반환되고 있었습니다.

그래서 한국사는 `_FAISS_EXCLUDED_SUBJECTS`로 지정해 FAISS를 건너뛰고,
손으로 작성한 ConceptMap 13개 항목을 **의미 유사도로 정렬**해 반환합니다
(`rag.py`의 `_concept_map_semantic`). 개념 설명은 어댑터가 없으므로
Ollama(`llama3.1:latest`)로 폴백합니다.

---

## RAG 검색 순서

`rag.py`의 `subject_aware_search`:

1. 개요 쿼리("핵심 개념 및 자주 출제되는 단원") → ConceptMap 과목 샘플
2. ConceptMap 키워드 매칭 → 히트하면 즉시 반환 (FAISS 섞지 않음)
3. 한국사 → ConceptMap 의미 유사도 상위 k개
4. FAISS 벡터 검색 + 과목 필터(경로 패턴 → 키워드 순)
5. 전부 실패 → ConceptMap 과목 샘플

ConceptMap을 FAISS보다 우선하는 것은 의도된 설계입니다. 손으로 정리한
압축 노트가 원본 코퍼스 조각보다 정보 밀도가 높기 때문입니다. ConceptMap에
없는 개념은 4번에서 FAISS가 정상적으로 처리합니다.

임베딩은 `jhgan/ko-sroberta-multitask`, 인덱스는 FAISS(정규화 + 내적)입니다.

---

## Ollama 의존성

어댑터가 없는 과목(한국사)과 어댑터 생성 실패 시 `localhost:11434`의
`llama3.1:latest`로 폴백합니다. **Ollama가 없으면 에러 없이 조용히 해당
섹션이 사라집니다** (`_ollama_analyze`가 `None`을 반환하고 호출부가 생략).
리포트에 "AI 개념 분석"이 안 보이면 Ollama부터 확인하세요.

---

## 알려진 과제

- **수학 어댑터 정확도** — 객관식 36%(무작위 20%)로 교과 어댑터에 크게 못 미친다.
  형식만 학습되고 수리 추론은 부족한 상태. 데이터 확대나 풀이 과정(CoT) 학습 등
  접근을 바꿔야 할 것으로 보인다.
- **개념 설명 품질 평가 부재** — 서빙에서 실제로 쓰는 경로인데 정량 지표가 없다.
  정답이 하나로 정해지지 않아 평가셋 설계부터 필요하다.
- 한국사 어댑터용 학습 데이터 확보 (현재 유일하게 파인튜닝 없는 과목)
- 글쓰기 채점 1점 클래스 문제 — 가중치·오버샘플링 둘 다 실패
- `rpi/pose_hailo.py`의 `decode_keypoints()` 미구현 — Hailo 실물에서
  `describe_outputs()`로 텐서 형태를 확인해야 진행 가능
